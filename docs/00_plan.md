# 00_plan.md — Master TDD Implementation Checklist

> Source: `docs/00_spec.md` · Authored: 2026-05-03 · Reorg: 2026-05-04 (Round 3)
> Workflow: `CLAUDE.md` §THE TDD WORKFLOW · Tidy First (`[STRUCTURAL]` / `[BEHAVIORAL]`)
> Each `[ ]` = one Red→Green→Refactor cycle, each cycle = one (or two) commit.
> Reorg driver: `docs/team/2026_05_04_phase1_round3_reorg_auth_off.md` (12/12 6-of-6).

## Status legend
- `[x]` delivered
- `[ ]` TODO
- `[~]` scaffolded-but-disabled (P1 OPEN mode — auth track deferred to P2)

---

## Phase 1 — Tracks (organized by domain)

> Tasks are grouped by domain track. The `Week` column preserves the original 5–7-week schedule for PM. The `Depends On` column lists prior task IDs that must be `[x]` first.

### Track T0 — Foundations (utilities & state machine)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T0.1 | Structural | • **Achieve:** Scaffold the initial project tree.<br>• **Deliver:** `pyproject.toml`, `src/ragent/`, `tests/{unit,integration,e2e}/`. | — | [x] | Dev | W1 |
| T0.2 | Structural | • **Achieve:** Lock in CI quality gate with coverage floor (DoD).<br>• **Deliver:** `make check` alias = `ruff format . && ruff check . --fix && pytest --cov=src/ragent --cov-branch --cov-fail-under=92`; CI fails on coverage drop. | T0.1 | [x] | Dev | W1 |
| T0.3 | Red | • **Achieve:** Pin sortable, URL-safe ID contract.<br>• **Deliver:** `tests/unit/test_id_gen.py` — `new_id()` returns 26-char Crockford base32; sortable across calls. | T0.1 | [x] | QA | W2 |
| T0.4 | Green | • **Achieve:** Implement UUIDv7-based ID generator.<br>• **Deliver:** `src/ragent/utility/id_gen.py` (UUIDv7 → 16 bytes → base32; ≤ 30 LOC). | T0.3 | [x] | Dev | W2 |
| T0.5 | Red | • **Achieve:** Pin end-to-end UTC datetime contract.<br>• **Deliver:** `tests/unit/test_datetime_utility.py` — `utcnow()` tz-aware UTC; `to_iso` ends in `Z`; `from_db` attaches UTC. | T0.1 | [x] | QA | W2 |
| T0.6 | Green | • **Achieve:** Implement UTC datetime helpers.<br>• **Deliver:** `src/ragent/utility/datetime.py`. | T0.5 | [x] | Dev | W2 |
| T0.7 | Red | • **Achieve:** Pin document state-machine transitions (S10).<br>• **Deliver:** `tests/unit/test_state_machine.py` — accepts `{UPLOADED→PENDING, PENDING→READY, PENDING→FAILED, PENDING→DELETING, READY→DELETING, FAILED→DELETING}`; rejects `{UPLOADED→FAILED, READY→PENDING, FAILED→READY, DELETING→READY}`. | T0.1 | [x] | QA | W2 |
| T0.8 | Structural | • **Achieve:** Establish persistent schema for documents + chunks (B3).<br>• **Deliver:** `migrations/001_initial.sql` — `documents` (cols: `document_id CHAR(26) PK`, `create_user VARCHAR(64) NOT NULL` (B14), `source_id VARCHAR(128) NOT NULL` (B11), `source_app VARCHAR(64) NOT NULL` (B11), `source_title VARCHAR(256) NOT NULL` (B11), `source_workspace VARCHAR(64) NULL` (B11), `object_key VARCHAR(256) NOT NULL` (B25), `status ENUM(...)`, `attempt INT`, `created_at`, `updated_at`) + `chunks` (cols: `chunk_id`, `document_id`, `ord`, `text MEDIUMTEXT`, `lang VARCHAR(8)`).<br>• Indexes: `idx_status_updated`, `idx_source_app_id_status_created`, `idx_create_user_document`, `idx_document` (chunks). Alembic configured. | T0.1 | [x] | Dev | W2 |
| T0.8a | Structural | • **Achieve:** Keep a head-of-tree schema snapshot for drift detection (B3).<br>• **Deliver:** `migrations/schema.sql` — consolidated snapshot updated in lockstep with every `NNN_*.sql`. | T0.8 | [x] | Dev | W2 |
| T0.8e | Structural | • **Achieve:** Check in canonical ES index definition (B26).<br>• **Deliver:** `resources/es/chunks_v1.json` — full settings + mappings per spec §5.2 (icu_text analyzer, bbq_hnsw vector index). | T0.1 | [x] | Dev | W2 |
| T0.9 | Structural | • **Achieve:** Provide reusable testcontainer fixtures for all integration tests (B8).<br>• **Deliver:** `tests/conftest.py` — session-scoped fixtures for MariaDB / ES / Redis / MinIO.<br>• ES container uses a custom image with `analysis-icu` plugin pre-installed (e.g. `Dockerfile.es-test` extending `docker.elastic.co/elasticsearch/elasticsearch:9.2.3`); fixture exposes `ES_HOSTS`, `ES_USERNAME`, `ES_PASSWORD` (or `ES_API_KEY`) and `ES_VERIFY_CERTS=false` (B28).<br>• MinIO fixture exposes `MINIO_ENDPOINT/ACCESS_KEY/SECRET_KEY`, `MINIO_SECURE=false` (B28). MariaDB fixture builds `MARIADB_DSN` (B28).<br>• Redis container starts in standalone mode by default; sentinel topology covered by a separate compose-style fixture when `REDIS_MODE=sentinel` (B27). | T0.1 | [x] | Dev | W2 |
| T0.8b | Red | • **Achieve:** Guarantee `schema.sql` ≡ `alembic upgrade head` (B3 invariant).<br>• **Deliver:** `tests/integration/test_schema_drift.py` — apply both to two scratch DBs (testcontainers); `mysqldump` diff must be empty. | T0.8a, T0.9 | [x] | QA | W2 |
| T0.8f | Red | • **Achieve:** Prevent prose/resource drift on ES index (B26).<br>• **Deliver:** `tests/integration/test_es_resource_drift.py` — parse `resources/es/chunks_v1.json` and the JSON block in spec §5.2; assert deep-equal. Pure file diff; no testcontainer needed. | T0.8e | [x] | QA | W2 |
| T0.8c | Red | • **Achieve:** Verify idempotent first-boot auto-init across MariaDB + ES (B3, B4).<br>• **Deliver:** `tests/integration/test_bootstrap_auto_init.py` — first boot creates tables/indexes/`chunks_v1` from `resources/es/chunks_v1.json` (B26); second boot is a no-op; pre-existing schema drift is logged `event=schema.drift` and surfaces in `/readyz` as degraded. | T0.8a, T0.8e, T0.9 | [x] | QA | W2 |
| T0.8d | Green | • **Achieve:** Implement non-destructive schema bootstrap.<br>• **Deliver:** `src/ragent/bootstrap/init_schema.py` — `CREATE … IF NOT EXISTS` for MariaDB; for ES, read every `resources/es/*.json` and `PUT /<index>` if absent (B26); refuses to `ALTER`. | T0.8c | [x] | Dev | W2 |
| T0.8g | Red | • **Achieve:** Fail closed when ES `analysis-icu` plugin is missing (B26, I5).<br>• **Deliver:** `tests/integration/test_es_plugin_required.py` — `/readyz` returns 503 with `error_code=ES_PLUGIN_MISSING` listing `analysis-icu`; init refuses to `PUT` the index. | T0.8d, T0.9 | [x] | QA | W2 |
| T0.10 | Structural | • **Achieve:** Provide one canonical TaskIQ broker dispatching on Redis topology (B27/B28).<br>• **Deliver:** `src/ragent/bootstrap/broker.py` — module-level `broker` export.<br>• Standalone reads `REDIS_BROKER_URL`; sentinel uses `REDIS_SENTINEL_HOSTS` + `REDIS_BROKER_SENTINEL_MASTER`. Single canonical import path `from ragent.bootstrap.broker import broker` used by every `@broker.task` decorator (T3.2b, T3.2d) and by the worker entrypoint (T7.5d). | T0.1 | [x] | Dev | W2 |
| T0.10a | Red | • **Achieve:** Pin broker topology dispatch behavior (B27).<br>• **Deliver:** `tests/unit/test_broker_topology.py` — standalone uses `REDIS_BROKER_URL`; sentinel uses `REDIS_SENTINEL_HOSTS` + `REDIS_BROKER_SENTINEL_MASTER`; missing var in sentinel mode → SystemExit. | T0.10 | [x] | QA | W2 |
| T0.11 | Structural | • **Achieve:** Single source of truth for operator env config (B30).<br>• **Deliver:** `.env.example` at repo root enumerating **every** variable from spec §4.6 in the same subsection order (bootstrap → datastore → redis → clients → worker → pipeline → timeouts → observability).<br>• Required vars carry `# REQUIRED — set before boot`; default-overridable vars show defaults inline. Operator workflow: `cp .env.example .env` → fill required → start API + worker. | T0.1 | [x] | Dev | W2 |
| T0.11a | Red | • **Achieve:** CI gate against operator-config drift (B30).<br>• **Deliver:** `tests/unit/test_env_example_drift.py` — parse spec §4.6 tables and `.env.example`; assert symmetric variable set, defaults match, every `(required)` var in spec is marked `# REQUIRED` in example. | T0.11 | [x] | QA | W2 |

### Track T1 — Plugins (Protocol + Registry + Extractors)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T1.1 | Red | • **Achieve:** Pin plugin Protocol shape (S4).<br>• **Deliver:** `tests/unit/test_plugin_protocol.py` — protocol attribute/method conformance. | T0.1 | [x] | QA | W2 |
| T1.2 | Green | • **Achieve:** Provide runtime-checkable plugin Protocol.<br>• **Deliver:** `src/ragent/plugins/protocol.py` (`runtime_checkable` Protocol). | T1.1 | [x] | Dev | W2 |
| T1.3 | Red | • **Achieve:** Pin stub graph extractor no-op behavior (S5).<br>• **Deliver:** Stub graph extractor no-op test. | T0.1 | [x] | QA | W2 |
| T1.4 | Green | • **Achieve:** Implement stub graph extractor placeholder for P1.<br>• **Deliver:** `src/ragent/plugins/stub_graph.py`. | T1.3 | [x] | Dev | W2 |
| T1.5 | Refactor | • **Achieve:** Confirm no premature abstraction across plugins (YAGNI).<br>• **Deliver:** Review note — no shared boilerplate; kept duplicated. | T1.4 | [x] | Reviewer | W2 |
| T1.6 | Red | • **Achieve:** Pin registry semantics: register, fan_out, timeout, dup-detect (S11, R6, S29).<br>• **Deliver:** `tests/unit/test_plugin_registry.py` — `register`, `fan_out`, `all_required_ok`; duplicate name raises; per-plugin timeout `PLUGIN_FAN_OUT_TIMEOUT_SECONDS` (B28, default 60) overrun → `Result(error="timeout")`. | T1.2 | [x] | QA | W3 |
| T1.7 | Green | • **Achieve:** Implement plugin registry with concurrent fan-out.<br>• **Deliver:** `src/ragent/plugins/registry.py` (`PluginRegistry`, `Result`, `DuplicatePluginError`); concurrent fan_out with per-plugin timeout from `PLUGIN_FAN_OUT_TIMEOUT_SECONDS`. | T1.6 | [x] | Dev | W3 |
| T1.8 | Red | • **Achieve:** Pin `fan_out_delete` semantics: idempotent, no DB tx held (R10, P-E).<br>• **Deliver:** `tests/unit/test_plugin_registry_delete.py` — calls every registered plugin; per-plugin timeout `PLUGIN_FAN_OUT_TIMEOUT_SECONDS` (B28); idempotent on already-deleted. | T1.7 | [x] | QA | W3 |
| T1.9 | Red | • **Achieve:** Pin VectorExtractor contract: idempotent ingest, clean delete.<br>• **Deliver:** `tests/unit/test_vector_extractor.py` — Protocol conformance, embedder/ES bulk once, idempotent rerun, delete clears chunks. | T1.2 | [x] | QA | W3 |
| T1.10 | Green | • **Achieve:** Implement vector extractor plugin.<br>• **Deliver:** `src/ragent/plugins/vector.py`. | T1.9 | [x] | Dev | W3 |
| T1.11 | Red | • **Achieve:** Pin title-aware embedding + DI shape + ES doc fields (B15+B17+B29).<br>• **Deliver:** `tests/unit/test_vector_extractor_title.py` — constructor `VectorExtractor(repo, chunks, embedder, es)` (B17 DI).<br>• Mock embedder asserts input equals `f"{source_title}\n\n{chunk_text}"`.<br>• ES bulk body carries `title`, `text`, `lang`, `chunk_id`, `document_id`, `embedding`, `source_app`, `source_workspace` per `resources/es/chunks_v1.json` (B26, B29) — no extra/missing fields.<br>• `source_workspace` omitted when NULL (ES `keyword` missing; `term` filter does not match). `extract(document_id)` reads denormalised values via `repo.get`. Constructor injection only — no service-locator, no globals. | T1.10, T2.2, T0.8e | [x] | QA | W3+ |
| T1.12 | Green | • **Achieve:** Amend extractor for title-prefixed embedding + denormalised fields (B17, B29).<br>• **Deliver:** Updated `src/ragent/plugins/vector.py` — constructor `(repo, chunks, embedder, es)`; `extract` fetches doc, prepends `f"{source_title}\n\n"` before embedding, writes `title`, `source_app`, `source_workspace` (when non-null) into ES bulk doc. | T1.11 | [x] | Dev | W3+ |

### Track T2 — Ingest CRUD (Repositories + Storage + Service + Router)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T2.1 | Red  | • **Achieve:** Pin DocumentRepository CRUD + lock + lifecycle queries (B11, B14, B16, B17, B25, B29, R1/R3/R7, S28/S33).<br>• **Deliver:** `tests/unit/test_document_repository.py`.<br>• **Coverage:** `create` (mandatory `source_id`+`source_app`+`source_title`+`create_user`+`object_key`, optional `source_workspace`); `get` returns DocumentRow with all denormalised fields; `delete`; `list`.<br>• **Concurrency:** `acquire_nowait` (FOR UPDATE NOWAIT raises on lock contention); `list_ready_by_source` and `pop_oldest_loser_for_supersede` FOR UPDATE SKIP LOCKED LIMIT 1 (P-C single-loser-per-tx).<br>• **Lifecycle:** `update_status` (state-machine guarded); `update_heartbeat` bumps `updated_at` to NOW() (B16, S33); `list_pending_stale`, `list_uploaded_stale` (R1).<br>• **Hydration:** `find_multi_ready_groups` (R3); `get_sources_by_document_ids` for chat hydration (C4 rename); `list_by_create_user` for future "my uploads" (B14). | T0.4, T0.6, T0.7 | [x] | QA | W3 |
| T2.2 | Green | • **Achieve:** Implement document persistence layer (CRUD only).<br>• **Deliver:** `src/ragent/repositories/document_repository.py`. | T2.1 | [x] | Dev | W3 |
| T2.3 | Red  | • **Achieve:** Pin chunk persistence contract.<br>• **Deliver:** `tests/unit/test_chunk_repository.py` — `bulk_insert` / `delete_by_document_id`. | T0.4 | [x] | QA | W3 |
| T2.4 | Green | • **Achieve:** Implement chunk persistence layer.<br>• **Deliver:** `src/ragent/repositories/chunk_repository.py`. | T2.3 | [x] | Dev | W3 |
| T2.5 | Red  | • **Achieve:** Pin MinIO client object-key format, timeouts, and key-only return contract (B10, B25, C3).<br>• **Deliver:** `tests/unit/test_minio_client.py` — `put_object(source_app, source_id, document_id, ...)` builds key `{source_app}_{source_id}_{document_id}` (sanitised to `[A-Za-z0-9._-]`) in bucket from `MINIO_BUCKET` (default `ragent`, B10/B28).<br>• Upload bound by `MINIO_PUT_TIMEOUT_SECONDS` (B28, default 60); download by `MINIO_GET_TIMEOUT_SECONDS` (B28, default 30). Returns object key only, not URI. `delete_object(key)` idempotent. Bucket read once at startup; MinIO is transient staging only. | T0.1 | [x] | QA | W3 |
| T2.6 | Green | • **Achieve:** Implement MinIO client adapter.<br>• **Deliver:** `src/ragent/storage/minio_client.py` — B10 key format, B25 returns key string; per-call timeouts from `MINIO_PUT_TIMEOUT_SECONDS` / `MINIO_GET_TIMEOUT_SECONDS` (B28). | T2.5 | [x] | Dev | W3 |
| T2.7 | Red  | • **Achieve:** Pin ingest-create service contract: validation, MIME allow-list, atomic put+create+kiq, rollback on MinIO failure (B11, B25, C1, S23).<br>• **Deliver:** `tests/unit/test_ingest_service_create.py` — `source_id`/`source_app`/`source_title` mandatory (422 on missing); MIME validated against `{text/plain, text/markdown, text/html, text/csv}` (≤50 MB); MinIO put → `repo.create` (persists `source_id`, `source_app`, `source_title`, `object_key`, `create_user`, optional `source_workspace`) → kiq dispatch; row rolled back if MinIO put fails. | T2.2, T2.6, T1.7 | [x] | QA | W3 |
| T2.8 | Green | • **Achieve:** Implement ingest-create service path.<br>• **Deliver:** `src/ragent/services/ingest_service.py::create` (≤ 30 LOC/method). | T2.7 | [x] | Dev | W3 |
| T2.9 | Red  | • **Achieve:** Pin delete cascade order, idempotency, no-tx-during-fan-out (P-E, S13/S14).<br>• **Deliver:** `tests/unit/test_ingest_service_delete.py` — order: acquire NOWAIT → DELETING short tx → `fan_out_delete` OUTSIDE tx → chunks → MinIO (if UPLOADED/PENDING) → row.<br>• Failure leaves row in DELETING (S13); re-delete returns 204 idempotently (S14); `fan_out_delete` runs with no DB tx open. | T2.8, T1.8 | [x] | QA | W3 |
| T2.10 | Green | • **Achieve:** Implement delete cascade.<br>• **Deliver:** `src/ragent/services/ingest_service.py::delete`. | T2.9 | [x] | Dev | W3 |
| T2.11 | Red | • **Achieve:** Pin cursor pagination + limit clamp (S15).<br>• **Deliver:** `tests/unit/test_ingest_service_list.py` — cursor by `document_id` ASC; `?limit` upper-bounded by `INGEST_LIST_MAX_LIMIT` (B28, default 100); `limit=200` clamped or 422 per contract; `next_cursor` correctness; P1 OPEN — no ACL filter. | T2.2 | [x] | QA | W3 |
| T2.12 | Green | • **Achieve:** Implement list service path.<br>• **Deliver:** `src/ragent/services/ingest_service.py::list`. | T2.11 | [x] | Dev | W3 |
| T2.13 | Red | • **Achieve:** Pin router as thin parse/validate/delegate layer with RFC 9457 errors (B5, B11, S23).<br>• **Deliver:** `tests/unit/test_ingest_router.py` — 415 `INGEST_MIME_UNSUPPORTED` outside C1; 413 `INGEST_FILE_TOO_LARGE` over `INGEST_MAX_FILE_SIZE_BYTES` (B28, default 52428800); 422 on missing `source_id`/`source_app`/`source_title`; `X-User-Id` required (P1 OPEN).<br>• All non-2xx bodies are `application/problem+json` with `error_code`; 422 carries `errors[]`. | T2.8, T2.10, T2.12 | [x] | QA | W3 |
| T2.14 | Green | • **Achieve:** Implement ingest router + RFC 9457 problem builder.<br>• **Deliver:** `src/ragent/routers/ingest.py` + `src/ragent/errors/problem.py` (B5); declares all endpoints in spec §4.1. | T2.13 | [x] | Dev | W3 |

### Track T3 — Pipelines (Ingest + Chat assembly)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T3.1 | Red | • **Achieve:** Pin language-routed ingest pipeline shape (B1).<br>• **Deliver:** `tests/integration/test_ingest_pipeline.py` — Haystack `Convert→Clean→LanguageRouter→{cjk_splitter\|en_splitter} (sentence)→Embed`; mock embedder. EN doc → `en_splitter`; CJK → `cjk_splitter`; chunk count == sentence count. | T2.4, T4.2 | [x] | QA | W3 |
| T3.2 | Green | • **Achieve:** Implement ingest pipeline factory.<br>• **Deliver:** `src/ragent/pipelines/factory.py` + `pipelines/ingest.py`. | T3.1 | [x] | Dev | W3 |
| T3.2a | Red | • **Achieve:** Pin terminal-commit-before-MinIO-delete order (S16, S21).<br>• **Deliver:** `tests/integration/test_worker_minio_cleanup.py` — terminal status (`READY`/`FAILED`) committed before `MinIOClient.delete_object`; delete failure → still terminal + `event=minio.orphan_object`; on retry path (still PENDING) MinIO object retained. | T3.2 | [x] | QA | W3 |
| T3.2b | Green | • **Achieve:** Implement ingest worker task with two-tx envelope, heartbeat, timeout, post-commit cleanup (B16, B18, B25, R5, S27).<br>• **Deliver:** `@broker.task("ingest.pipeline")` (broker from T0.10).<br>• Flow: TX-A acquire NOWAIT + PENDING (commit) → start heartbeat (UPDATE `updated_at` every `WORKER_HEARTBEAT_INTERVAL_SECONDS`) → pipeline body OUTSIDE tx, `asyncio.wait_for(timeout=PIPELINE_TIMEOUT_SECONDS)` → cancel heartbeat in `finally` → TX-B terminal commit (FAILED also runs `fan_out_delete` + `delete_by_document_id`; PIPELINE_TIMEOUT sets `error_code=PIPELINE_TIMEOUT`) → post-commit MinIO best-effort delete.<br>• Plugin registry + repos resolved via composition root (T7.5a). | T3.2a, T0.10 | [x] | Dev | W3 |
| T3.2i | Red | • **Achieve:** Verify heartbeat suppresses live-worker re-dispatch (S33, B16).<br>• **Deliver:** `tests/integration/test_worker_heartbeat.py` — mid-pipeline 4-min sleep + stand-in Reconciler query at 5 min sees `updated_at < 30 s`, row excluded; on worker kill, `updated_at` ages past 5 min and same query returns row exactly once. | T3.2b, T2.2 | [x] | QA | W3 |
| T3.2j | Red | • **Achieve:** Pin pipeline-timeout failure path (S34, B18).<br>• **Deliver:** `tests/integration/test_pipeline_timeout.py` — body sleeping > `PIPELINE_TIMEOUT_SECONDS` → row `FAILED` with `error_code=PIPELINE_TIMEOUT`, cleanup ran, `event=ingest.failed reason=pipeline_timeout` logged. | T3.2b | [x] | QA | W3 |
| T3.2k | Red | • **Achieve:** Pin CSV row-merger branch keyed on MIME (S35, B24).<br>• **Deliver:** `tests/integration/test_csv_row_merger.py` — 10 000-row CSV (~50 chars each) → `chunks` count ≤ `ceil(total_chars / CSV_CHUNK_TARGET_CHARS)` (≈ 250); identical-size `.txt` produces sentence-level chunks. | T3.2 | [x] | QA | W3 |
| T3.2l | Green | • **Achieve:** Implement CSV-only RowMerger branch (B24).<br>• **Deliver:** Pipeline factory adds `RowMerger` SuperComponent on `text/csv` branch only (`ConditionalRouter` on MIME); merges rows joined by `\n` until buffer ≥ `CSV_CHUNK_TARGET_CHARS`. | T3.2k | [x] | Dev | W3 |
| T3.2c | Red | • **Achieve:** Pin supersede semantics: per-loser commits, MAX(created_at) survives, scoped by `(source_id, source_app)` (P-C, S17–S22, S31).<br>• **Deliver:** `tests/integration/test_supersede_task.py` — on `READY` kiqs `ingest.supersede`; pops oldest loser one-at-a-time, commits per-loser; same `source_id` different `source_app` coexists; out-of-order finish converges; FAILED never enqueues supersede; idempotent re-run. | T3.2b, T2.10 | [x] | QA | W3 |
| T3.2d | Green | • **Achieve:** Implement supersede service + worker without holding K row locks across K cascades.<br>• **Deliver:** `services/ingest_service.py::supersede(document_id)` + `@broker.task("ingest.supersede")`; loops `pop_oldest_loser_for_supersede` + cascade delete + commit. | T3.2c, T0.10 | [x] | Dev | W3 |
| T3.2e | Red | • **Achieve:** Guarantee retry idempotency — no duplicate chunks (R4, S25).<br>• **Deliver:** `tests/integration/test_pipeline_retry_idempotent.py` — Reconciler retry of partial ingest produces zero duplicates; pipeline first step is `delete_by_document_id` + `VectorExtractor.delete`. | T3.2 | [x] | QA | W3 |
| T3.2f | Green | • **Achieve:** Implement idempotency-clean prefix.<br>• **Deliver:** Pipeline factory prepends idempotency-clean step. | T3.2e | [x] | Dev | W3 |
| T3.2g | Red | • **Achieve:** Pin acquire-NOWAIT contention path: fail fast, no `attempt` increment (R7, S28).<br>• **Deliver:** `tests/unit/test_worker_acquire_nowait.py` — concurrent dispatch: second worker fails fast, re-kiqs self with backoff, `attempt` unchanged. | T3.2 | [x] | QA | W3 |
| T3.2h | Green | • **Achieve:** Implement NOWAIT + exponential backoff retry.<br>• **Deliver:** Worker uses `acquire_nowait`; on `LockNotAvailable` re-kiqs with exponential backoff (cap 30 s). | T3.2g | [x] | Dev | W3 |
| T3.3 | Red | • **Achieve:** Pin chat request validation, env-driven defaults, system auto-prepend, optional filters (B12, B22, B29, S6b/S6c/S6i).<br>• **Deliver:** `tests/unit/test_chat_request_schema.py` — `messages` required (422 `CHAT_MESSAGES_MISSING`); defaults from `RAGENT_DEFAULT_LLM_PROVIDER/MODEL/TEMPERATURE/MAX_TOKENS`; `provider` allow-list `{"openai"}` else 422 `CHAT_PROVIDER_UNSUPPORTED`; auto-prepend `RAGENT_DEFAULT_SYSTEM_PROMPT` if no system entry.<br>• Filters: `source_app` / `source_workspace` optional; both omitted ⇒ no filter; empty or > 64 chars ⇒ 422 `CHAT_FILTER_INVALID`. | T4.6 | [x] | QA | W4 |
| T3.4 | Green | • **Achieve:** Implement `ChatRequest` schema + helpers per B21/B22/B29.<br>• **Deliver:** `src/ragent/schemas/chat.py` (Pydantic) — `ChatRequest` with env-driven defaults + provider allow-list validator + optional `source_app`/`source_workspace` (length 1–64, snake_case); `Message`; `normalize_messages()` prepends default system. JSON naming per B21 (`maxTokens` camelCase; ID/scope fields snake_case). | T3.3 | [x] | Dev | W4 |
| T3.5 | Red | • **Achieve:** Pin hybrid retrieval pipeline: title-aware embedding, BM25 over `icu_text`, filters, source hydration returning full chunk content (B11, B15, B23, B26, B29, C4, S6f–S6h, S36).<br>• **Deliver:** `tests/integration/test_chat_pipeline_retrieval.py`.<br>• **Components:** `QueryEmbedder` → `ESVector` kNN + `ESBM25 multi_match(["text","title^2"])` with `icu_text` → `DocumentJoiner` (`CHAT_JOIN_MODE`) → `SourceHydrator` joins `documents` for `(source_app, source_id, source_title)` and **returns full chunk content untruncated** (B23 revised: truncation is router-layer only).<br>• **Recall:** title-only lexical recalls via BM25 `title^2`; semantic title query recalls via title-aware embedding (B15).<br>• **CJK BM25 (S36):** `"產品規格"` recalls via `icu_tokenizer` and not via `standard` analyzer control.<br>• **Filters (S6f–S6h):** `source_app` set ⇒ both retrievers carry `term: {source_app: <v>}`; combined with `source_workspace` ANDs; non-matching ⇒ empty (`sources: null` at router); empty index ⇒ empty. | T2.4, T4.2, T4.4, T2.2, T1.12 | [x] | QA | W4 |
| T3.5a | Red | • **Achieve:** Pin pipeline-graph dispatch by `CHAT_JOIN_MODE` (C6).<br>• **Deliver:** `tests/unit/test_pipeline_factory_join_mode.py` — for each value ∈ {`rrf`, `concatenate`, `vector_only`, `bm25_only`} factory returns expected component graph (e.g. `vector_only` has no BM25 retriever and no joiner). Default `rrf`. | T3.5 | [x] | QA | W4 |
| T3.6 | Green | • **Achieve:** Implement chat retrieval pipeline + shared retrieval utilities (B23 revised).<br>• **Deliver:** `src/ragent/pipelines/chat.py::build_retrieval_pipeline(join_mode)` + `SourceHydrator` (returns full chunk content; truncation moved to router layer) + shared `build_es_filters(source_app, source_workspace)` + `doc_to_source_entry(doc)` utilities; `EXCERPT_MAX_CHARS` exported as public constant. Factory dispatches on `CHAT_JOIN_MODE`. | T3.5, T3.5a | [x] | Dev | W4 |
| T3.7 | Red | • **Achieve:** Pin non-streaming LLM client contract (B28).<br>• **Deliver:** `tests/unit/test_llm_client_chat.py` — `LLMClient.chat(messages,...)` returns `{content, usage:{promptTokens,completionTokens,totalTokens}}`; honours `model`/`temperature`/`maxTokens`; timeout `LLM_TIMEOUT_SECONDS` (default 120). | T4.6 | [x] | QA | W4 |
| T3.8 | Green | • **Achieve:** Implement non-streaming `chat()` and ensure streaming surfaces usage.<br>• **Deliver:** `src/ragent/clients/llm.py::chat()` (timeout `LLM_TIMEOUT_SECONDS`); streaming requests `stream_options.include_usage=true` so terminal `done` carries usage. | T3.7 | [x] | Dev | W4 |
| T3.9 | Red | • **Achieve:** Pin `POST /chat` non-streaming response shape (B5, B12, B13, S6a/S6d/S6e).<br>• **Deliver:** `tests/integration/test_chat_endpoint.py` — 200 `application/json` per §3.4.2 (`content`, `usage`, `model`, `provider`, `sources[]`); each `sources[]` has 6 fields + `type="knowledge"`; empty retrieval ⇒ `sources: null`; LLM error ⇒ RFC 9457 problem+json. | T3.6, T3.8 | [x] | QA | W4 |
| T3.10 | Green | • **Achieve:** Implement non-streaming chat endpoint.<br>• **Deliver:** `src/ragent/routers/chat.py::POST /chat`. | T3.9 | [x] | Dev | W4 |
| T3.11 | Red | • **Achieve:** Pin SSE streaming framing (B6, B12, S6).<br>• **Deliver:** `tests/integration/test_chat_stream_endpoint.py` — emits ≥1 `data: {"type":"delta",...}` then exactly one `data: {"type":"done", ...§3.4.2 body...}`; mid-stream LLM failure → single `data: {"type":"error","error_code","message"}` then close (no `event: error`); pre-stream failure ⇒ RFC 9457 problem+json. | T3.6, T3.8 | [x] | QA | W4 |
| T3.12 | Green | • **Achieve:** Implement SSE chat stream endpoint.<br>• **Deliver:** `src/ragent/routers/chat.py::POST /chat/stream` — delta/done/error framing per §3.4.3. | T3.11 | [x] | Dev | W4 |
| T3.13 | Red | • **Achieve:** Pin fixed-window per-key rate-limit primitive against Redis (B27, B31).<br>• **Deliver:** `tests/unit/test_rate_limiter.py` — `check(key, limit, window_seconds) → RateLimitResult(allowed, remaining, reset_at)`; under limit ⇒ allowed, at limit ⇒ blocked + `reset_at = now + remaining_window`; window expiry resets. Uses `INCR` + `EXPIRE` against `REDIS_RATELIMIT_*`; standalone + sentinel covered. | T0.10, T0.9 | [x] | QA | W4 |
| T3.14 | Green | • **Achieve:** Implement RateLimiter adapter + composition wiring (B31).<br>• **Deliver:** `src/ragent/clients/rate_limiter.py` reading `REDIS_RATELIMIT_URL` (standalone) or `REDIS_RATELIMIT_SENTINEL_MASTER` + `REDIS_SENTINEL_HOSTS` (sentinel) from composition root; key prefix `ratelimit:`. Composition root exports `chat_rate_limit_dep` `Depends` factory bound to `CHAT_RATE_LIMIT_PER_MINUTE` / `CHAT_RATE_LIMIT_WINDOW_SECONDS`. | T3.13 | [x] | Dev | W4 |
| T3.15 | Red | • **Achieve:** Pin per-user rate-limit behavior end-to-end (B31, S37).<br>• **Deliver:** `tests/integration/test_chat_rate_limit.py` — with `CHAT_RATE_LIMIT_PER_MINUTE=N`, N+1 calls from same `X-User-Id`: first N → 200, (N+1)th → 429 problem+json `CHAT_RATE_LIMITED` + `Retry-After`; same for `/chat/stream`. Different user gets fresh budget; window expiry resets counter. | T3.14, T3.10, T3.12 | [x] | QA | W4 |
| T3.16 | Green | • **Achieve:** Apply rate-limit dependency to chat surfaces only (B31).<br>• **Deliver:** Wire `Depends(chat_rate_limit_dep)` onto chat router for `/chat` and `/chat/stream`; health/ingest/MCP NOT rate-limited (router-level dependency, not global middleware). | T3.15 | [x] | Dev | W4 |
| T3.17 | Red | • **Achieve:** Pin RAG message construction: grounding system message + `=== CONTEXT START/END ===` user wrap.<br>• **Deliver:** `tests/unit/test_build_rag_messages.py` + extensions to chat endpoint/stream tests.<br>• **Behavior:** when retrieval ≥1 doc, LLM receives leading grounded system message + last user wrapped with context delimiters then verbatim query; each rendered chunk includes `source_app`, `source_title`, `document_id`, excerpt.<br>• **Edge cases:** with user-supplied system message, grounding rules prepend as a SEPARATE system message; empty retrieval ⇒ T3.3 default-system path with user message NOT wrapped. | T3.10, T3.12 | [x] | QA  | W4 |
| T3.18 | Green | • **Achieve:** Implement RAG message builder + wire into routers.<br>• **Deliver:** `src/ragent/schemas/chat.py::build_rag_messages` + `_render_context` + `_wrap_last_user`; routers swap `normalize_messages` for `build_rag_messages` and run retrieval before message construction. New env vars `RAGENT_DEFAULT_RAG_SYSTEM_PROMPT` and `RAGENT_RAG_GROUNDING_RULES` (defaults wired; `.env.example` updated). | T3.17 | [x] | Dev | W4 |
| T3.19 | Red | • **Achieve:** Pin standalone retrieve endpoint: filters, dedupe, excerpt truncated at router (§3.4.4, B23, S38–S41).<br>• **Deliver:** `tests/unit/test_retrieve_router.py` — `query` required; `source_app`/`source_workspace` optional (same B29 semantics, same length validators as ChatRequest); `dedupe=false` → all ranked chunks (same `document_id` may repeat); `dedupe=true` → one chunk per `document_id` (highest-scored, first-seen in score-sorted output); empty ⇒ `{"chunks":[]}` (not `null`); `excerpt` truncated to `EXCERPT_MAX_CHARS` in router. | T3.6, T3.10 | [x] | QA | W4 |
| T3.20 | Green | • **Achieve:** Implement `POST /retrieve` using shared pipeline utilities.<br>• **Deliver:** `src/ragent/routers/retrieve.py` — `RetrieveRequest` (`query`, `source_app`, `source_workspace`, `dedupe: bool = False`, same filter validators as `ChatRequest`); uses `build_es_filters` + `doc_to_source_entry` from `pipelines/chat`; `_dedupe_by_document` post-step; wired into `bootstrap/app.py` via `create_retrieve_router(retrieval_pipeline)`. | T3.19 | [x] | Dev | W4 |

### Track T4 — Third-Party Clients

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T4.1 | Red | • **Achieve:** Pin token-manager refresh boundary, single-flight, J1-key body, ISO-8601 expiry, K8s SA mode, and credential safety (S9, P-F).<br>• **Deliver:** `tests/unit/test_token_manager.py` — POSTs `{"key": j1_token}` to `AI_API_AUTH_URL/auth/api/accesstoken`; parses `{"token": …, "expiresAt": "ISO-8601"}` response; refreshes at `expiresAt − 5min` boundary using fake clock; 100 concurrent calls share one HTTP exchange; K8s SA mode reads token from file; J1 token never appears in exception text. | T0.6 | [x] | QA | W4 |
| T4.2 | Green | • **Achieve:** Implement TokenManager with single-flight refresh per rule.md §"LLM & Embedding & Re-rank Auth API".<br>• **Deliver:** `src/ragent/clients/auth.py` (`TokenManager`) — `j1_token` or `k8s_sa_token_path`; `{"key": j1}` POST body; ISO-8601 `expiresAt` parse; `threading.Lock` single-flight. `composition.py` wires 3 separate managers (`AI_LLM/EMBEDDING/RERANK_API_J1_TOKEN`) or single shared SA manager when `AI_USE_K8S_SERVICE_ACCOUNT_TOKEN=true`. | T4.1 | [x] | Dev | W4 |
| T4.3 | Red | • **Achieve:** Pin embedding-client batching, asymmetric ingest/query timeouts, retry policy (B28, C8, P-B).<br>• **Deliver:** `tests/unit/test_embedding_client.py` — POST shape, `bge-m3`, validates `returnCode == 96200`, retry 3× @ 1 s.<br>• Batch interface accepts `list[str]`; one HTTP call per batch up to `EMBEDDER_BATCH_SIZE` (default 32). Ingest bound by `EMBEDDER_INGEST_TIMEOUT_SECONDS` (default 30); query bound by `EMBEDDER_QUERY_TIMEOUT_SECONDS` (default 10). | T4.2 | [x] | QA | W4 |
| T4.4 | Green | • **Achieve:** Implement embedding client with two timeout paths.<br>• **Deliver:** `src/ragent/clients/embedding.py` — batch size from `EMBEDDER_BATCH_SIZE`; ingest/query call paths use `EMBEDDER_INGEST_TIMEOUT_SECONDS` / `EMBEDDER_QUERY_TIMEOUT_SECONDS`. | T4.3 | [x] | Dev | W4 |
| T4.5 | Red | • **Achieve:** Pin LLM streaming contract + retry policy (B28).<br>• **Deliver:** `tests/unit/test_llm_client.py` — streaming async iterator yields deltas; timeout `LLM_TIMEOUT_SECONDS` (default 120); retry 3× @ 2 s. | T4.2 | [x] | QA | W4 |
| T4.6 | Green | • **Achieve:** Implement streaming LLM client.<br>• **Deliver:** `src/ragent/clients/llm.py` (timeout from `LLM_TIMEOUT_SECONDS`). | T4.5 | [x] | Dev | W4 |
| T4.7 | Red | • **Achieve:** Pin rerank client request shape (wired in P2).<br>• **Deliver:** `tests/unit/test_rerank_client.py` — POST shape, `bge-reranker-base`, `top_k=2`. | T4.2 | [x] | QA | W4 |
| T4.8 | Green | • **Achieve:** Implement rerank client.<br>• **Deliver:** `src/ragent/clients/rerank.py`. | T4.7 | [x] | Dev | W4 |

### Track T5 — Resilience (Reconciler)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T5.1 | Red | • **Achieve:** Pin PENDING re-dispatch + heartbeat suppression (S2, S33).<br>• **Deliver:** `tests/integration/test_reconciler_redispatch.py` — PENDING row with `updated_at < NOW() - RECONCILER_PENDING_STALE_SECONDS` (B16/B28, default 5 min) → re-kiq, idempotent; live worker with fresh `updated_at` is NOT re-dispatched. | T2.2, T2.8 | [x] | QA | W6 |
| T5.2 | Green | • **Achieve:** Implement one-shot Reconciler with K8s CronJob (B9).<br>• **Deliver:** `src/ragent/reconciler.py` — entrypoint `python -m ragent.reconciler`; `SELECT … FOR UPDATE SKIP LOCKED`. Manifest `deploy/k8s/reconciler-cronjob.yaml` (`*/5 * * * *`, `concurrencyPolicy: Forbid`) checked in. | T5.1 | [x] | Dev | W6 |
| T5.3 | Red | • **Achieve:** Pin attempt-budget exhaustion → FAILED with alert (S3).<br>• **Deliver:** `tests/integration/test_reconciler_failed.py` — `attempt > WORKER_MAX_ATTEMPTS` (B28, default 5) → status=FAILED + structured-log alert. | T5.2 | [x] | QA | W6 |
| T5.4 | Green | • **Achieve:** Implement FAILED transition + alert log line.<br>• **Deliver:** Status transition + structured log line `event=ingest.failed`. | T5.3 | [x] | Dev | W6 |
| T5.5 | Red | • **Achieve:** Pin DELETING resume idempotency (S13).<br>• **Deliver:** `tests/integration/test_reconciler_delete_resume.py` — DELETING row with `updated_at < NOW() - RECONCILER_DELETING_STALE_SECONDS` (B28, default 300) → resume cascade idempotently. | T2.10 | [x] | QA | W6 |
| T5.6 | Green | • **Achieve:** Implement DELETING resume.<br>• **Deliver:** Reconciler resumes DELETING. | T5.5 | [x] | Dev | W6 |
| T5.7 | Red | • **Achieve:** Pin UPLOADED-orphan re-dispatch (R1, S24).<br>• **Deliver:** `tests/integration/test_reconciler_uploaded_orphan.py` — UPLOADED row with `updated_at < NOW() - RECONCILER_UPLOADED_STALE_SECONDS` (B28, default 300) → re-kiq `ingest.pipeline`. | T2.8 | [x] | QA | W6 |
| T5.8 | Green | • **Achieve:** Implement UPLOADED-stale arm.<br>• **Deliver:** Reconciler arm for `UPLOADED > 5 min`. | T5.7 | [x] | Dev | W6 |
| T5.9 | Red | • **Achieve:** Pin multi-READY repair via supersede (R3, S26).<br>• **Deliver:** `tests/integration/test_reconciler_multi_ready_repair.py` — two READY rows for same `(source_id, source_app)` → re-enqueue `ingest.supersede`. | T3.2d | [x] | QA | W6 |
| T5.10 | Green | • **Achieve:** Implement multi-READY detector arm.<br>• **Deliver:** Reconciler arm `GROUP BY source_id, source_app HAVING COUNT(*)>1` → kiq supersede. | T5.9 | [x] | Dev | W6 |
| T5.11 | Red | • **Achieve:** Guarantee FAILED leaves no partial chunks/ES (R5, S27).<br>• **Deliver:** `tests/integration/test_reconciler_failed_cleanup.py` — on `attempt>5 → FAILED`, partial chunks/ES cleared. | T5.4 | [x] | QA | W6 |
| T5.12 | Green | • **Achieve:** Wire fan-out cleanup into FAILED transition.<br>• **Deliver:** FAILED transition runs `fan_out_delete` + `delete_by_document_id` before commit. | T5.11 | [x] | Dev | W6 |
| T5.13 | Red | • **Achieve:** Pin Reconciler tick observability (R8, S30).<br>• **Deliver:** `tests/integration/test_reconciler_heartbeat.py` — every tick increments `reconciler_tick_total` and emits `event=reconciler.tick`. | T5.2 | [x] | QA | W6 |
| T5.14 | Green | • **Achieve:** Implement tick counter + log line.<br>• **Deliver:** Heartbeat counter + log line in `reconciler.py`. | T5.13 | [x] | Dev | W6 |

### Track T6 — MCP Schema (501 in P1)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T6.1 | Structural | • **Achieve:** Reserve MCP surface in P1 with explicit 501 (§4.1.2).<br>• **Deliver:** `src/ragent/routers/mcp.py` exposing `POST /mcp/tools/rag` that returns 501 `application/problem+json` with `error_code=MCP_NOT_IMPLEMENTED`; mounted by `create_app` (T7.5c); OpenAPI auto-generated from FastAPI signature. | T2.14 | [x] | Dev | W6 |
| T6.2 | Red | • **Achieve:** Pin P1 MCP 501 contract (S8).<br>• **Deliver:** `tests/unit/test_mcp_endpoint.py` — returns 501 in P1. | T6.1 | [x] | QA | W6 |

### Track T7 — Observability + Acceptance

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T7.1 | Refactor | • **Achieve:** Wire OTEL traces + Prometheus metrics across api/worker/reconciler.<br>• **Deliver:** Haystack auto-trace + FastAPI middleware (no custom spans). OTLP exporter from `OTEL_EXPORTER_OTLP_ENDPOINT` (B28; export disabled when unset); service name from `OTEL_SERVICE_NAME` per process (`ragent-api` / `ragent-worker` / `ragent-reconciler`); sampling from `OTEL_TRACES_SAMPLER` + `OTEL_TRACES_SAMPLER_ARG`.<br>• Logs include `auth_mode=open` field. Counters: `reconciler_tick_total`, `minio_orphan_object_total`, `multi_ready_repaired_total`. Histogram: `worker_pipeline_duration_seconds`. | T3.4 | [x] | SRE | W6 |
| T7.1a | Red | • **Achieve:** Pin alerting on stalled Reconciler ticks (R8, S30).<br>• **Deliver:** `tests/integration/test_alerting_rules.py` — Prometheus rule fires when `reconciler_tick_total` flat for > 10 min. | T7.1 | [x] | QA | W6 |
| T7.1b | Behavioral | • **Achieve:** Adopt structlog for ISO 8601 / API trace / business / error logs with OTEL correlation across api/worker/reconciler and chat/retrieve flows; pin Haystack 2.x content tracing off (Logging Rule).<br>• **Deliver:** `src/ragent/bootstrap/logging_config.py` (`configure_logging`, `LOG_FORMAT=json\|console`, denylist redaction); `src/ragent/middleware/logging.py` (`RequestLoggingMiddleware`); router-level OTEL spans + business logs in `routers/chat.py` and `routers/retrieve.py`; client spans in `clients/{llm,embedding,rerank}.py`; `setup_tracing` pins `is_content_tracing_enabled` (gated by `HAYSTACK_CONTENT_TRACING_ENABLED`).<br>• Tests: `tests/unit/test_logging_config.py`, `test_logging_redaction.py`, `test_logging_middleware.py`, `test_chat_retrieve_tracing.py`, `test_clients_tracing.py`. | T7.1 | [x] | SRE | W7 |
| T7.2 | Acceptance | • **Achieve:** Validate the operator quickstart promise end-to-end (B30).<br>• **Deliver:** `tests/e2e/test_ingest_success_rate.py` — fresh testcontainers + populated `.env` (from `.env.example`, T0.11) → launch real `python -m ragent.api` (T7.5d) and `python -m ragent.worker` (T7.5e) as subprocesses (no test-only scaffolding) → POST 100 documents to `/ingest` → poll `/ingest/{id}` → ≥ 99% reach `READY` within 60 s each.<br>• Asserts: only steps required are `cp .env.example .env`, fill required vars, start API + worker. | T3.2, T5.6, T7.5d, T7.5e, T0.11 | [x] | QA | W6 |
| T7.3 | Acceptance | • **Achieve:** Hit golden-set retrieval target on the live API process (C7).<br>• **Deliver:** `tests/e2e/test_golden_set.py` — top-3 ≥ 70% on 50 Q/A/expected_doc_id rows from `tests/e2e/golden_set.jsonl`; invoked against the same launched API process as T7.2 (`POST /chat` at `http://127.0.0.1:8000/chat`). | T3.4, T7.2 | [x] | QA | W6 |
| T7.4 | Acceptance | • **Achieve:** Validate chaos recovery SLA.<br>• **Deliver:** Chaos drill: kill worker mid-ingest → Reconciler recovers ≤ 10 min. | T5.6 | [x] | SRE | W6 |
| T7.5 | Structural | • **Achieve:** Refuse to boot in any unsafe P1 OPEN configuration (B28).<br>• **Deliver:** `src/ragent/bootstrap/guard.py` — refuses to start unless `RAGENT_AUTH_DISABLED=true AND RAGENT_ENV=dev`; reads `RAGENT_HOST` (default `127.0.0.1`, refuses non-loopback in P1 OPEN) + `RAGENT_PORT` (default 8000); configures `LOG_LEVEL` (default `INFO`) for app + TaskIQ + Reconciler logging. Imported by both api and worker entrypoints. | T2.14 | [x] | SRE | W6 |
| T7.5a | Structural | • **Achieve:** Single composition root that owns all env reads + DI graph (B17, B30, B31, C6).<br>• **Deliver:** `src/ragent/bootstrap/composition.py`.<br>• **Singletons:** `TokenManager`, `EmbeddingClient`, `LLMClient`, `RerankClient`, `MinIOClient`, `Elasticsearch`, MariaDB engine, `RateLimiter`.<br>• **Wiring:** instantiates `DocumentRepository` / `ChunkRepository`; constructs plugins with DI args (`VectorExtractor(repo, chunks, embedder, es)`, `StubGraphExtractor()`); registers required + optional plugins onto one `PluginRegistry`; builds chat pipeline factory parameterised by `CHAT_JOIN_MODE` (C6).<br>• **Exports:** `Container` dataclass + `get_container()` accessor consumed by FastAPI `Depends` (T2.14, T3.10, T3.12, T7.8), worker tasks (T3.2b, T3.2d), Reconciler (T5.2); `chat_rate_limit_dep` `Depends` factory bound to `CHAT_RATE_LIMIT_PER_MINUTE` / `CHAT_RATE_LIMIT_WINDOW_SECONDS` (B31).<br>• **Invariant:** all env-var reads happen here, not inside business modules. | T1.7, T1.12, T2.2, T2.4, T2.6, T3.6, T3.8, T3.14, T4.2, T4.4, T4.6, T4.8 | [x] | Dev | W6 |
| T7.5b | Red | • **Achieve:** Verify composition root builds a fully-wired graph eagerly (B17, B30).<br>• **Deliver:** `tests/integration/test_composition_root.py` — `container.registry.plugins["vector"]` is a `VectorExtractor` whose `__init__` received the same `repo`/`chunks`/`embedder`/`es` instances exposed on the container (DI verified by identity); `container.chat_pipeline_factory(join_mode="rrf")` returns a runnable Haystack pipeline; missing required env var → SystemExit at construction time. | T7.5a | [x] | QA | W6 |
| T7.5c | Structural | • **Achieve:** Implement the FastAPI app factory with lifespan-driven init + RFC 9457 errors + `X-User-Id` middleware (B5, C9).<br>• **Deliver:** `src/ragent/bootstrap/app.py::create_app() -> FastAPI` — calls `guard.enforce()` (T7.5), constructs `Container` (T7.5a), registers `lifespan` hook that runs `init_schema()` (T0.8d) before traffic, mounts routers (`ingest` T2.14, `chat` T3.10/T3.12, `mcp` T6.x, `health` T7.8), wires `Depends`, installs problem+json error handler and `X-User-Id` middleware that excludes `/livez`/`/readyz`/`/metrics`. | T7.5, T7.5a, T0.8d, T2.14, T3.10, T3.12, T7.8 | [x] | Dev | W6 |
| T7.5d | Structural | • **Achieve:** Provide the single API entrypoint operators run.<br>• **Deliver:** `src/ragent/api.py` (`python -m ragent.api`) — uvicorn launcher: imports `bootstrap.app:create_app`, binds to `RAGENT_HOST:RAGENT_PORT`, log level from `LOG_LEVEL`. | T7.5c | [x] | Dev | W6 |
| T7.5e | Structural | • **Achieve:** Provide the single worker entrypoint operators run.<br>• **Deliver:** `src/ragent/worker.py` (`python -m ragent.worker`) — calls `guard.enforce()`, constructs the same `Container` (T7.5a), imports `ragent.bootstrap.broker:broker` (T0.10) and task modules (`ragent.workers.ingest`, `ragent.workers.supersede`) so `@broker.task` decorators register, runs the broker. Calls `init_schema()` defensively (idempotent) so worker can start before API. | T0.10, T7.5a, T0.8d, T3.2b, T3.2d | [x] | Dev | W6 |
| T7.5f | Red | • **Achieve:** Verify app factory boots end-to-end with lifespan + middleware.<br>• **Deliver:** `tests/integration/test_app_factory.py` — `create_app()` against testcontainers boots; lifespan runs `init_schema` exactly once; routers mounted at expected paths; `/livez` reachable without `X-User-Id`; protected route returns 422 problem+json on missing `X-User-Id`. | T7.5c | [x] | QA | W6 |
| T7.6 | Red | • **Achieve:** Pin startup-guard refusals.<br>• **Deliver:** `tests/unit/test_bootstrap_startup_guard.py` — non-dev env or `RAGENT_AUTH_DISABLED` unset → SystemExit; `RAGENT_HOST` other than `127.0.0.1` while `RAGENT_AUTH_DISABLED=true` → SystemExit; invalid `LOG_LEVEL` → SystemExit; happy path boots with all three vars at defaults. | T7.5 | [x] | QA | W6 |
| T7.7 | Red | • **Achieve:** Pin health/metrics surfaces and dependency probes (B4, B26, B27, B28, C9).<br>• **Deliver:** `tests/integration/test_health_endpoints.py`.<br>• **Endpoints:** `GET /livez` → 200 always; `GET /readyz` → 200 only when all probes pass within `READYZ_PROBE_TIMEOUT_SECONDS` (default 2 s); `GET /metrics` → 200 `text/plain; version=0.0.4`.<br>• **Probes:** MariaDB (`SELECT 1`); ES (cluster health + `analysis-icu` plugin + every `resources/es/*.json` index exists); Redis broker (`PING` against `REDIS_MODE` topology); Redis rate-limiter (`PING`, separate instance); MinIO (`ListBuckets`).<br>• **Failure mapping:** one dep down → 503 problem+json with `error_code` matching `ES_PLUGIN_MISSING` / `ES_INDEX_MISSING` / `SCHEMA_DRIFT` per §4.1.2 catalog.<br>• **Metrics exposed:** `reconciler_tick_total`, `worker_pipeline_duration_seconds`, `minio_orphan_object_total`, `multi_ready_repaired_total`.<br>• **Middleware bypass (C9):** all three endpoints reachable WITHOUT `X-User-Id`. | T7.1 | [x] | QA | W6 |
| T7.8 | Green | • **Achieve:** Implement health + metrics endpoints with per-dep timeouts and middleware bypass (B27, B28, C9).<br>• **Deliver:** `src/ragent/routers/health.py` (`/livez`, `/readyz`, `/metrics`); probes via `asyncio.wait_for(..., timeout=READYZ_PROBE_TIMEOUT_SECONDS)`; broker + rate-limiter Redis pinged independently per `REDIS_*_URL` / `REDIS_*_SENTINEL_MASTER`; `X-User-Id` middleware excludes the three health paths. | T7.7 | [x] | Dev | W6 |

### Track T8 — Authentication & Permission Layer `[~] DISABLED IN P1`

> P1 produces NO code in this track. Authentication (JWT) and Permission (OpenFGA via `PermissionClient` Protocol) are **separate layers**; ES carries no auth fields in any phase (B14). Interface in `00_spec.md` §3.5; implementation lands in P2 (see Phase 2 row P2.2).

| # | Category | Task | Depends On | Status | Owner | Phase |
|---|---|---|---|:---:|---|:---:|
| T8.0 | Structural | • **Achieve:** Centralise the `X-User-Id` literal in `bootstrap/app.py` so the BEHAVIORAL T8.2 commit only changes behaviour, not literal spelling.<br>• **Deliver:** `src/ragent/bootstrap/app.py` — replace the two `"X-User-Id"` literals in `_x_user_id_middleware` with a single module-level constant matching `middleware/logging.py:_USER_ID_HEADER`. Pre/post test runs identical. | (entry) | [x] | Dev | P2 |
| T8.1 | Red  | • ✱ **Superseded by T8.1a (2026-05-20).** Decode-only contract dropped; signature verification reinstated via Armasec.<br>• **Achieve:** Pin decode-only JWT validation contract (§3.5 / §3.5.1) for all `/chat*` / `/ingest*` / `/retrieve*` / `/feedback*` / `/mcp*` endpoints.<br>• **Deliver:** `tests/unit/test_jwt.py` — JWT shape `{"exp": <unix>, "<RAGENT_JWT_CLAIM_USER_ID>": "<user_id>"}`: expired `exp` → 401 `AUTH_TOKEN_EXPIRED`; missing `exp` or claim → 401 `AUTH_CLAIM_MISSING`; non-numeric `exp` → 401 `AUTH_TOKEN_INVALID`; malformed token (not three base64url segments / payload not JSON / payload not object) → 401 `AUTH_TOKEN_INVALID`. **Signature is intentionally not verified** (§3.5.1 accepted risk); no test asserts signature behavior. Custom claim name (`RAGENT_JWT_CLAIM_USER_ID=sub`) round-trip covered. | T8.0 | [x] | QA | P2 |
| T8.1a | Red | • **Achieve:** Pin Armasec-verified JWT contract (§3.5 rewritten 2026-05-20) for all protected endpoints, plus public-path bypass.<br>• **Deliver:** (a) `tests/conftest.py` — fake OIDC fixtures piggy-backed on `armasec.pytest_extension` (auto-loaded via `pytest_armasec` entry point; ships `rs256_*`, `mock_openid_server`, `build_rs256_token` — pre-generated RS256 keypair, no per-test keygen). Local conftest adds `armasec_token_manager` (composes `OpenidConfigLoader → TokenDecoder → TokenManager` inside the respx-mocked OIDC server, then yields the manager AFTER the mock context exits — any subsequent JWKS fetch attempt hits real network and fails, implicitly pinning the cache-reuse invariant of §3.5) and `make_token` (wraps `build_rs256_token` to default `aud` to the test audience). No real network in any path. (b) `tests/unit/test_armasec_auth.py` — happy path (valid token → `user_id` extracted, claim default `preferred_username`, custom claim override works); bad signature → 401 `AUTH_TOKEN_INVALID`; wrong `aud` → 401 `AUTH_TOKEN_INVALID`; wrong `iss` → 401 `AUTH_TOKEN_INVALID`; expired `exp` → 401 `AUTH_TOKEN_EXPIRED`; missing user_id claim → 401 `AUTH_CLAIM_MISSING`; absent `<RAGENT_JWT_HEADER>` header → 401 `AUTH_TOKEN_INVALID`; (c) `tests/unit/test_armasec_public_paths.py` — each of `/livez /readyz /startupz /metrics /docs /docs/oauth2-redirect /redoc /openapi.json` bypasses the middleware even without a token; one-shot assertion `_SKIP_PATHS ⊂ _PUBLIC_PATHS` pins the superset relationship. | T8.0 | [x] | QA | P2 |
| T8.2 | Green | • ✱ **Superseded by T8.2a (2026-05-20).** Decode-only implementation replaced by Armasec verifier.<br>• **Achieve:** Implement decode-only JWT middleware + configurable inbound headers (§3.5).<br>• **Deliver:** `src/ragent/auth/jwt.py` — `decode_jwt_payload(token: str, *, claim_user_id: str, now: int) -> str` returns `user_id`; raises `JwtAuthError(error_code, http_status=401)` on every failure path enumerated in T8.1. `bootstrap/app.py::_x_user_id_middleware` accepts `user_id_header`, `jwt_header`, `jwt_claim`, `trust_header`, `auth_disabled` as wiring params; in JWT mode the extracted `user_id` is injected into `<user_id_header>` of `request.scope["headers"]` so downstream routers (`Header(alias=user_id_header)`) and `RequestLoggingMiddleware` observe it. Defaults preserve current P1 behaviour (auth_disabled=true). | T8.1 | [x] | Dev | P2 |
| T8.2a | Green | • **Achieve:** Replace decode-only with Armasec JWKS verification, header convention preserved.<br>• **Deliver:** (a) `pyproject.toml` — add `armasec` dep. (b) `src/ragent/auth/jwt.py` — drop `decode_jwt_payload`; expose `verify_jwt(token: str, *, claim_user_id: str, token_manager: VerifyingTokenManager) -> str` that wraps the raw token in `Headers({"Authorization": f"Bearer {token}"})` and calls `token_manager.manager.extract_token_payload(...)`, enforces `iss` matches the OIDC issuer (Armasec doesn't verify iss out of the box), then extracts the configured claim from the returned `TokenPayload`. Raises `JwtAuthError(error_code, http_status=401)` for each failure class enumerated in T8.1a. Also expose `build_token_manager(*, domain, audience, use_https) -> VerifyingTokenManager` (a thin `(manager, expected_iss)` dataclass — `expected_iss` precomputed once to avoid per-request normalization) that composes `OpenidConfigLoader(domain, use_https) → TokenDecoder(loader.jwks) → TokenManager(loader.config, decoder, audience)` — this is the single seam to Armasec internals so the rest of the codebase imports nothing from `armasec.*`. OIDC discovery + JWKS are fetched at composition (fail-fast on misconfig). (c) `src/ragent/bootstrap/composition.py` — call `build_token_manager(domain=ARMASEC_DOMAIN, audience=ARMASEC_AUDIENCE, use_https=ARMASEC_USE_HTTPS)` once and inject into the middleware factory. (d) `bootstrap/app.py::_x_user_id_middleware` — accept the `VerifyingTokenManager` instance and the public-path frozenset; in JWT mode call `verify_jwt(...)` and inject `<user_id_header>` exactly as before. P1 defaults (`auth_disabled=true`) untouched. (e) **Test migration** — delete `tests/unit/test_jwt.py` (decode-only contract retired); rewrite the JWT branches in `tests/unit/test_x_user_id_middleware.py` (and any other test that constructed a base64url token directly) to use the fake-OIDC fixture from T8.1a, asserting the same 401 codes through the Armasec path. No coverage regression. (f) **Operator-facing docs** — `.env.example` gets the three new env vars (`ARMASEC_DOMAIN`, `ARMASEC_AUDIENCE`, `ARMASEC_USE_HTTPS`) with the same defaults/required-when wording as §4.6.1. (README points users to `.env.example` rather than listing vars inline; no README edit needed.) | T8.1a | [x] | Dev | P2 |
| T8.3a | Red+Green | • **Achieve:** Codify the public-path bypass as a single named constant; pin its relationship to the structured-logging skip set with one test assertion.<br>• **Deliver:** `src/ragent/bootstrap/app.py` — `_PUBLIC_PATHS = frozenset({"/livez","/readyz","/startupz","/metrics","/docs","/docs/oauth2-redirect","/redoc","/openapi.json"})` consumed by `_x_user_id_middleware`. Strict superset of `middleware/logging.py::_SKIP_PATHS`; the relationship is pinned by a one-shot test assertion `_SKIP_PATHS <= _PUBLIC_PATHS` in `tests/unit/test_oidc_public_paths.py` (no runtime check — assertion cost is paid once at test collection). `middleware/logging.py::_SKIP_PATHS` is NOT refactored to import `_PUBLIC_PATHS` because its set is intentionally narrower (operational logs only, not auth surface). | T8.2a | [x] | Dev | P2 |
| T8.5a | Structural | • **Achieve:** Replace Armasec (no httpx DI seam, can't pass `verify=False` / custom CA) with **joserfc** (the actively-maintained successor to `authlib.jose`) + explicit `httpx.Client` injection. Contract preserved — same `verify_jwt` / `build_token_manager` signatures, same `VerifyingTokenManager` carrier, same `AUTH_TOKEN_*` codes; all 1151 unit tests pass unchanged after fixture rename.<br>• **Deliver:** (a) `pyproject.toml` — drop `armasec` + `[tool.uv] override-dependencies` (the `pytest>=8` override was only there for Armasec's `pytest<9` pin); add `joserfc>=1.6.4`. Net dep tree 199 → 187. (b) `src/ragent/auth/jwt.py` — `build_token_manager(*, domain, audience, use_https, verify_ssl, client) -> VerifyingTokenManager` with `client: httpx.Client \| None` as the DI seam; production uses default client; tests inject `httpx.MockTransport`; ops can pass a CA-bundle client. `VerifyingTokenManager` now carries `(jwks: KeySet, audience, expected_iss)`. `verify_jwt` uses `joserfc.jwt.decode(...)` + `joserfc.jwt.JWTClaimsRegistry` for aud / exp / nbf / iat and the existing rstrip-tolerant iss check. (c) `src/ragent/bootstrap/composition.py` — env vars renamed `ARMASEC_*` → `OIDC_*`; new `OIDC_VERIFY_SSL` (default `true`). (d) `.env.example` + `docs/00_spec.md` §3.5 + §4.6.1 mirror the rename and add `OIDC_VERIFY_SSL` documentation (dev-only false; for prod private CA use `SSL_CERT_FILE`). (e) `tests/conftest.py` — self-contained fake OIDC (module-level RSA-2048 keygen + `httpx.MockTransport` handler) replacing the `armasec.pytest_extension` borrow. Fixture renamed `armasec_token_manager` → `oidc_token_manager`. `tests/unit/test_armasec_auth.py` → `test_oidc_auth.py`; `tests/unit/test_armasec_public_paths.py` → `test_oidc_public_paths.py`. No coverage regression. | T8.3a | [x] | Dev | P2 |
| T8.3 | Red  | • **Achieve:** Pin Permission Protocol surface.<br>• **Deliver:** `tests/unit/test_permission_client_protocol.py` — Protocol conformance for `batch_check(user_id, document_ids, relation) -> set[str]` and `list_objects(user_id, relation) -> list[str] \| None`. | T8.2 | [~] | QA | P2 |
| T8.4 | Green | • **Achieve:** Implement Permission Protocol + OpenFGA adapter (B14).<br>• **Deliver:** `src/ragent/auth/permission.py` — `PermissionClient` Protocol + `OpenFGAPermissionClient` (sole module importing the OpenFGA SDK). | T8.3 | [~] | Dev | P2 |
| T8.5 | Red  | • **Achieve:** Pin chat permission gate as opt-in post-retrieval filter; ES carries no auth fields (B14).<br>• **Deliver:** `tests/integration/test_chat_permission_gate.py` — with `RAGENT_PERMISSION_CHAT_ENABLED=true`, retrieval over-fetches K' candidates; `PermissionClient.batch_check(user_id=preferred_username, …)` post-filters to allowed; user A's query never surfaces user B's private doc. Flag `false` (default) bypasses gate. ES query body carries NO auth filter in either case. | T8.4 | [~] | QA | P2 |
| T8.6 | Green | • **Achieve:** Wire opt-in chat permission gate.<br>• **Deliver:** `pipelines/chat.py` post-retrieval gate (between SourceHydrator and LLM, or before hydration); no-op unless `RAGENT_PERMISSION_CHAT_ENABLED=true`. | T8.5 | [~] | Dev | P2 |
| T8.7 | Red  | • **Achieve:** Pin ingest permission gate behavior + `create_user` always recorded.<br>• **Deliver:** `tests/unit/test_ingest_permission.py` — with `RAGENT_PERMISSION_INGEST_ENABLED=true`, `GET /ingest/{id}` and `DELETE /ingest/{id}` call `batch_check([id])` and 403 problem+json when not allowed; `GET /ingest` filters via `list_objects` (or `batch_check` fallback). Flag `false` (default) leaves all three endpoints unrestricted. `documents.create_user` is set from `preferred_username` on POST regardless of flag. | T8.4 | [~] | QA | P2 |
| T8.8 | Green | • **Achieve:** Wire opt-in ingest permission gate at three call sites.<br>• **Deliver:** `services/ingest_service.py::get/delete`; `list` calls `list_objects` then `WHERE document_id IN (...)`, falling back to per-page `batch_check` when `list_objects` returns `None`. All no-op unless `RAGENT_PERMISSION_INGEST_ENABLED=true`. | T8.7 | [~] | Dev | P2 |
| T8.9 | Behavioral | • **Achieve:** Resolve employee identity + write OpenFGA tuples on ingest.<br>• **Deliver:** `HRClient` + JWT `preferred_username` → employee resolution; OpenFGA tuple-write on ingest (`user:<preferred_username>` viewer of `document:<doc_id>`). | T8.2, T8.4 | [~] | Dev | P2 |

---

## Definition of Done — Phase 1

> Closed 2026-05-10 against `claude/phase1-review-phase2-prep-COZZR` HEAD = `origin/main@42781a3` + the conftest broker-monkeypatch fix (`tests/conftest.py` + `tests/unit/test_worker_decoration_invariant.py`). Evidence column points at the test or doc that backs each tick.

- [x] Every `[ ]` row in tracks T0–T7 is `[x]`. _T7.3.x and T7.4.x relocated to `## Phase 1 → Phase 2 spillover` below — both are explicitly P2-owned acceptance gates against live AI endpoints / chaos lanes that cannot run inside the per-PR CI gate._
- [x] T8 rows remain `[~]` (auth disabled by design); spec §3.5 and §4.5 still describe the P2 contract. _Verified — all nine T8.* rows still `[~]`; track header still flagged `DISABLED IN P1`._
- [x] `uv run ruff format . && uv run ruff check . && uv run pytest` exits 0. _Last full run 2026-05-10: `make test-gate` = 626 passed / 0 failed / 0 errored (60 conditional skips on `pytest.mark.docker` paths covered by other arms); ruff format = "176 files already formatted"; ruff check = "All checks passed!"_
- [x] **TDD test coverage > 92%** — `uv run pytest --cov=src/ragent --cov-fail-under=92` passes; CI rejects PRs that drop below the floor. Coverage is line + branch. _2026-05-10: 93.47% with branch coverage; `--cov-fail-under=92` exit 0._
- [x] Acceptance metrics T7.2 / T7.3 / T7.4 met. _T7.2 (`tests/e2e/test_ingest_success_rate.py`) green; T7.3 (`tests/e2e/test_golden_set.py`) green against WireMock embedder per PR + spilled to T7.3.x for live-endpoint gate; T7.4 (`tests/e2e/test_chaos_worker_kill.py`) green per local invocation + spilled to T7.4.x for partial-failure suite. Both spillovers tracked under `## Phase 1 → Phase 2 spillover`._
- [x] Startup guard (T7.5) verified (`pytest tests/unit/test_bootstrap_startup_guard.py`). _Green; non-dev env / non-loopback host / invalid LOG_LEVEL all SystemExit per T7.6._
- [x] Every BDD scenario in `00_spec.md` §3.X has a corresponding plan row whose test path matches. _Spec defines 50 `S<n>` IDs. Each maps to a plan row — most cited verbatim; the rest covered under range citations (`S6g` via T3.5's `S6f–S6h`; `S18`/`S19`/`S20` via T3.2c's `S17–S22, S31`; `S39`/`S40` via T3.19's `S38–S41`) or via a named test path (`S1` 1 MB `.txt` READY-within-60 s → `tests/e2e/test_ingest_success_rate.py` under T7.2; `S12` DELETE cascade → `tests/integration/test_http_delete_runs_fan_out.py` under T-RR.11; `S32` ES freshness window → T7.2's 60 s SLA per the `S1 acceptance` cross-reference on `S32` in spec §3.1). No orphan unbacked scenarios._
- [x] `00_journal.md` carries at least one P1 lesson per domain encountered. _All six domains (Architecture, SRE, QA, Security, Spec, Process) have ≥ 4 entries each as of 2026-05-10._

---

## Phase 1 → Phase 2 spillover

> Two acceptance gates promised in P1 cannot run inside the per-PR CI gate as-is — both need infrastructure (live AI endpoints / slow-job chaos lane) that is P2-owned. Recorded here so DoD §200 stays an absolute invariant for tracks T0–T7 and the spillovers stay visible.

| # | Category | Task | Depends On | Status | Owner | Phase |
|---|---|---|---|:---:|---|:---:|
| T7.3.x | Acceptance | • **Achieve:** Wire the T7.3 retrieval-recall SLO to a real automated gate against live embedder/rerank/LLM endpoints (closes the gap noted in `docs/00_journal.md::E2E gate integrity`).<br>• **Deliver:** (a) Scheduled / label-triggered CI job that exports real `EMBEDDING_API_URL` / `LLM_API_URL` / `RERANK_API_URL` + tokens via secrets, runs `make test-e2e-golden`; (b) decision-log row pinning which endpoint identity is used + cost expectation per run; (c) `tests/e2e/test_golden_set.py::test_golden_set_top3_accuracy_at_least_70pct` xfail flips to a hard assertion on that job. Default WireMock e2e remains the per-PR gate. | T7.3 | [ ] | QA | P2 |
| T7.4.x | Acceptance | • **Achieve:** Replace the single happy-path chaos test (currently xfail run=False, `tests/e2e/test_chaos_worker_kill.py`) with a partial-failure suite covering the cross-storage failure modes that motivated the test.<br>• **Deliver:** decomposed into Track T-CHAOS rows C1–C6 below; this row stays for traceability and flips `[x]` when all six are green. | T5.6, T7.4 | [ ] | SRE | P2 |

---

## Track T-MCP — MCP JSON-RPC 2.0 Server (P2.5) — 2026-05-11

> Spec: `00_spec.md` §3.8 (B47). Implements `POST /mcp/v1` JSON-RPC 2.0 endpoint with five methods (`initialize`, `notifications/initialized`, `tools/list`, `tools/call`, `ping`) and one tool `retrieve` wrapping `POST /retrieve/v1`. P1 stub `POST /mcp/v1/tools/rag` is removed in T-MCP.12.

| # | Category | Task | Depends On | Status | Owner |
|---|---|---|---|:---:|---|
| T-MCP.1 | Red | • **Achieve:** Pin JSON-RPC 2.0 envelope contract (parse error, invalid request, method not found, notification).<br>• **Deliver:** `tests/unit/test_mcp_envelope.py` — (a) malformed JSON → 200 body `{jsonrpc:"2.0",id:null,error:{code:-32700,...}}`; (b) missing `jsonrpc`/`method` → -32600; (c) unknown method → -32601; (d) notification (`id` absent) → HTTP 204 empty body (S64/S65). | — | [x] | QA |
| T-MCP.2 | Green | • **Achieve:** Implement JSON-RPC dispatcher skeleton.<br>• **Deliver:** `src/ragent/routers/mcp.py` — `create_mcp_router()` returns router exposing `POST /mcp/v1`; envelope parsing + method dispatch table; envelope helpers `_jsonrpc_result(id, result)` / `_jsonrpc_error(id, code, message, data=None)`; `ping` handler returns `{}`. Handler signatures coroutines so T-MCP.8 can await the retrieval pipeline. Body-size cap deferred (production ingress bounds + future spec §3.8.1 pin). | T-MCP.1 | [x] | Dev |
| T-MCP.3 | Red | • **Achieve:** Pin `initialize` handshake (S58).<br>• **Deliver:** `tests/unit/test_mcp_initialize.py` — request `{method:"initialize", params:{protocolVersion:"2024-11-05", capabilities:{}, clientInfo:{...}}}` → result `{protocolVersion:"2024-11-05", capabilities:{tools:{}}, serverInfo:{name:"ragent", version:<semver>}}`. | T-MCP.2 | [x] | QA |
| T-MCP.4 | Green | • **Achieve:** Implement `initialize` handler.<br>• **Deliver:** `mcp.py::_handle_initialize(params)` returning the pinned `_MCP_PROTOCOL_VERSION="2024-11-05"` and `_MCP_SERVER_NAME="ragent"` module-level constants (B47 pins these; not env-driven). Server version reads `ragent.__version__`. | T-MCP.3 | [x] | Dev |
| T-MCP.5 | Red | • **Achieve:** Pin `tools/list` contract (S59) — exactly one tool `retrieve` with inputSchema matching §3.8.3.<br>• **Deliver:** `tests/unit/test_mcp_tools_list.py` — asserts `result.tools` length 1 and inputSchema deep-equals the spec literal (including `required:["query"]`, `top_k` bounds, etc.). | T-MCP.2 | [x] | QA |
| T-MCP.6 | Green | • **Achieve:** Implement `tools/list` returning the retrieve tool.<br>• **Deliver:** `mcp.py::_RETRIEVE_TOOL_SCHEMA` constant + `_handle_tools_list()`. | T-MCP.5 | [x] | Dev |
| T-MCP.7 | Red | • **Achieve:** Pin `tools/call retrieve` happy path (S60).<br>• **Deliver:** `tests/unit/test_mcp_tools_call_retrieve.py` — mock retrieval pipeline returns 3 chunks; request `{method:"tools/call", params:{name:"retrieve", arguments:{query:"q",top_k:3}}}` → `result.isError=false`, `result.content[0].type="text"`, `json.loads(result.content[0].text) == {chunks:[...]}` of length ≤ 3. | T-MCP.2 | [x] | QA |
| T-MCP.8 | Green | • **Achieve:** Implement `tools/call` dispatching to `run_retrieval`.<br>• **Deliver:** `mcp.py::_handle_tools_call` closure inside `create_mcp_router(retrieval_pipeline)` — name=retrieve dispatches to `run_retrieval(...)` via `run_in_threadpool`; serialises chunks via `doc_to_source_entry`; wraps as MCP content array. `app.py` now passes `container.retrieval_pipeline` like `create_retrieve_router`. | T-MCP.7 | [x] | Dev |
| T-MCP.9 | Red | • **Achieve:** Pin all `tools/call` error paths (S62, S63, S67) in one file.<br>• **Deliver:** `tests/unit/test_mcp_tools_call_errors.py` — (a) unknown tool name → `-32602 data.error_code=MCP_TOOL_NOT_FOUND`; (b) missing `query` → `-32602 data.error_code=MCP_TOOL_INPUT_INVALID`; (c) `top_k=0` (below min) → `-32602`; (d) mock pipeline raises `RuntimeError` → `-32001 data.error_code=MCP_TOOL_EXECUTION_FAILED` (NOT `isError:true` inside a success response — S67's app-error-vs-soft-error distinction). | T-MCP.8 | [x] | QA |
| T-MCP.10 | Green | • **Achieve:** Add input schema validation + tool name dispatch + pipeline-failure mapper.<br>• **Deliver:** `mcp.py::_validate_retrieve_args(args)` manual checks against `_RETRIEVE_TOOL_SCHEMA`; `_McpToolError` exception raised by handlers, caught by dispatcher and mapped to JSON-RPC error envelope; `_handle_tools_call` wraps pipeline call in try/except → `-32001` with `data.error_code=MCP_TOOL_EXECUTION_FAILED`; structlog `mcp.tool.error`. | T-MCP.9 | [x] | Dev |
| T-MCP.11 | Red | • **Achieve:** End-to-end through TestClient + real `build_retrieval_pipeline` (mocked components).<br>• **Deliver:** `tests/integration/test_mcp_router.py` — mounts router via `create_app`-like factory, runs `initialize → tools/list → tools/call retrieve` round-trip against a real Elasticsearch testcontainer; asserts response Content-Type `application/json` and that retrieved chunks include the seeded document_ids (proves the wire path actually reaches `run_retrieval`). Also pins `initialize → notifications/initialized → 204` standard client flow. | T-MCP.4, T-MCP.6, T-MCP.10 | [x] | QA |
| T-MCP.12 | Refactor | • **Achieve:** Remove P1 stub endpoint and update docs.<br>• **Deliver:** delete `POST /mcp/v1/tools/rag` 501 route + its unit test `tests/unit/test_mcp_endpoint.py`; remove `MCP_NOT_IMPLEMENTED` from `HttpErrorCode` enum and §4.1.2 catalog; `docs/API.md` documents `/mcp/v1` JSON-RPC. | T-MCP.11 | [x] | Dev |

> **S66 (auth) coverage:** the JWT/`X-User-Id` enforcement for `POST /mcp/v1` is delegated to Track T8 (Authentication & Permission Layer) — once T8.x lands, the same auth dependency that gates `/chat/v1` and `/retrieve/v1` also covers `/mcp/v1`. No separate T-MCP row.

---

## Track T-CHAOS — Chaos Drill Suite (P2.6 軌三 / T7.4.x) — 2026-05-11

> Spec: `00_spec.md` §3.6.1 (B49). Each case under `tests/e2e/test_chaos/test_C<N>_<scenario>.py`, marked `@pytest.mark.docker`, gated by nightly CI lane (not per-PR). Common asserts per §3.6.1: terminal status; ES/DB consistency; OTEL spans; `chaos_drill_outcome_total{case,outcome}` increment.

| # | Category | Task | Depends On | Status | Owner |
|---|---|---|---|:---:|---|
| T-CHAOS.0 | Structural | • **Achieve:** Establish chaos suite scaffold and pin fixture-reuse policy.<br>• **Deliver:** `tests/e2e/test_chaos/__init__.py` + `tests/e2e/test_chaos/conftest.py` — shared fixtures: WireMock reset between cases, `chaos_drill_outcome_total` metric scrape helper. **Fixture-reuse policy** (avoid 6× ~30s testcontainer boot tax): C3/C4/C5/C6 (WireMock-only injection) reuse session-scoped `running_stack` from `tests/e2e/conftest.py`; C1/C2 (worker kill / split-brain) use function-scope `spawn_module` because they kill the worker process. `chaos_drill_outcome_total` counter added to `src/ragent/bootstrap/metrics.py` with labels `case`, `outcome`. | T7.4 | [x] | SRE |
| T-CHAOS.C1 | Red+Green | • **Achieve:** Validate worker SIGKILL recovery (existing test unblocked).<br>• **Deliver:** Move `tests/e2e/test_chaos_worker_kill.py` → `tests/e2e/test_chaos/test_C1_worker_sigkill.py` ✓; lift `xfail(run=False)` and write the four `@spec §3.6.1` assertions ✓; xfail(strict=True) lifted on 2026-05-14 after `_claim` was rewritten as `_atomic_claim` (single atomic conditional UPDATE; PENDING → PENDING is now a rowcount=1 outcome, no `assert_transition`). See journal SRE 2026-05-14 "Recovery semantics closeout". | T-CHAOS.0 | [x] | SRE |
| T-CHAOS.C2 | Red+Green | • **Achieve:** Validate worker crash between MariaDB commit and ES bulk leaves a recoverable state.<br>• **Deliver:** `tests/e2e/test_chaos/test_C2_db_es_split.py` — monkeypatch worker `_commit_ready` to raise `ConnectionError` post-DB-commit, pre-ES-bulk; restart worker; assert reconciler heals via R3 multi-READY-repair or worker retry; final state READY with ES chunks present. | T-CHAOS.0 | [ ] | SRE |
| T-CHAOS.C3 | Red+Green | • **Achieve:** Validate ES bulk 207 partial failure is retried idempotently.<br>• **Deliver:** `tests/e2e/test_chaos/test_C3_es_bulk_207.py` — WireMock proxy ES injects `errors:true` with 5/50 items failed on first attempt; worker retries failed items only (OVERWRITE policy); final 50 chunks; `event=es.bulk_partial_failure` log emitted. | T-CHAOS.0 | [ ] | SRE |
| T-CHAOS.C4 | Red+Green | • **Achieve:** Validate rerank 5xx fail-open (depends on P2.3 reranker wired).<br>• **Deliver:** `tests/e2e/test_chaos/test_C4_rerank_5xx.py` — WireMock `/rerank` returns 500 for 3 consecutive calls; chat returns 200 with RRF-ordered sources; `rerank_degraded_total{reason="5xx"}+=3`. Blocked on P2.3. | P2.3 | [ ] | SRE |
| T-CHAOS.C5 | Red+Green | • **Achieve:** Validate LLM stream interrupt emits `data: {type:"error",...}` per B6.<br>• **Deliver:** `tests/e2e/test_chaos/test_C5_llm_stream_interrupt.py` — WireMock streams 3 SSE `delta` events then closes TCP; client receives `data: {type:"error",error_code:"LLM_STREAM_INTERRUPTED",...}`; no 500 in API logs. | T-CHAOS.0 | [ ] | SRE |
| T-CHAOS.C6 | Red+Green | • **Achieve:** Validate MinIO transient 503 is retried (3×@2s built-in).<br>• **Deliver:** `tests/e2e/test_chaos/test_C6_minio_503.py` — WireMock proxy injects 503 on `GET /staging/{key}` for first 2 attempts; worker succeeds on attempt 3; final READY; `minio.transient_error` log count = 2. | T-CHAOS.0 | [ ] | SRE |
| T-CHAOS.7 | Structural | • **Achieve:** Wire nightly CI lane for chaos suite.<br>• **Deliver:** `.github/workflows/chaos-nightly.yml` runs `pytest tests/e2e/test_chaos -m docker` on a `cron: '0 3 * * *'` schedule (03:00 UTC); `Makefile` target `make test-chaos`; nightly artefact retains test logs for 30 days via `actions/upload-artifact`. | T-CHAOS.C1–C6 | [ ] | SRE |
| T7.4.x | Closure | • **Achieve:** Flip the spillover row when all six cases green for ≥ 3 consecutive nightly runs.<br>• **Deliver:** plan.md row `T7.4.x` → `[x]` with evidence (nightly run links). | T-CHAOS.7 | [ ] | SRE |

---

## Track TA — aiomysql Adoption (async DB layer) — 2026-05-06

> Decision doc: `docs/team/2026_05_06_aiomysql_adoption.md`
> Goal: replace blocking `pymysql` + sync engine with `aiomysql` + `AsyncEngine` throughout the FastAPI/TaskIQ async path.

| # | Category | Task | Status | Owner |
|---|---|---|:---:|---|
| TA.1 | Red | • **Achieve:** Pin async contract for DocumentRepository and ChunkRepository.<br>• **Deliver:** `tests/unit/test_async_repository_contract.py` — asserts all public methods are coroutine functions (`asyncio.iscoroutinefunction`); `AsyncMock` engine; `await repo.create/get/update_status/delete/list/...` all pass. | [x] | QA |
| TA.2 | Green | • **Achieve:** Convert both repos to `async def` with SQLAlchemy `AsyncEngine`.<br>• **Deliver:** `src/ragent/repositories/document_repository.py` + `chunk_repository.py` — `async with self._engine.begin() as conn: await conn.execute(...)`. | [x] | Dev |
| TA.3 | Red | • **Achieve:** Pin async IngestService contract.<br>• **Deliver:** Updated `tests/unit/test_ingest_service_create.py`, `test_ingest_service_delete.py`, `test_ingest_service_list.py` — all service methods `async def`; `await svc.create/get/delete/list/supersede`. | [x] | QA |
| TA.4 | Green | • **Achieve:** Convert IngestService to async.<br>• **Deliver:** `src/ragent/services/ingest_service.py` — all methods `async def`; `await self._repo.*` and `await self._chunks.*`. | [x] | Dev |
| TA.5 | Red | • **Achieve:** Pin router drops `run_in_threadpool`.<br>• **Deliver:** Updated `tests/unit/test_ingest_router.py` — routes call `await svc.method()` without thread dispatch. | [x] | QA |
| TA.6 | Green | • **Achieve:** Simplify ingest router to direct `await` calls.<br>• **Deliver:** `src/ragent/routers/ingest.py` — remove `run_in_threadpool`; routes are pure `async def` with direct `await svc.*`. | [x] | Dev |
| TA.7 | Red | • **Achieve:** Pin reconciler fully-async contract.<br>• **Deliver:** Updated reconciler tests — all `_mark_failed`, `_redispatch_pending`, `_redispatch_uploaded`, `_resume_deleting`, `_repair_multi_ready` are `async def`; `AsyncMock` repos. | [x] | QA |
| TA.8 | Green | • **Achieve:** Convert Reconciler to fully async.<br>• **Deliver:** `src/ragent/reconciler.py` — all methods `async def`; `await self._repo.*` and `await self._chunks.*`. | [x] | Dev |
| TA.9 | Red | • **Achieve:** Pin ingest worker direct `await` on repos (no `to_thread.run_sync` for repo calls).<br>• **Deliver:** Updated `tests/integration/test_worker_minio_cleanup.py` + `test_worker_acquire_nowait.py` with async repo mocks. | [x] | QA |
| TA.10 | Green | • **Achieve:** Refactor ingest worker to `await` repos directly + pipeline bridge via `anyio.from_thread.run`.<br>• **Deliver:** `src/ragent/workers/ingest.py` — async task `await repo.*`; pipeline Haystack components use `anyio.from_thread.run(async_fn, *args)` bridge; heartbeat uses `asyncio.new_event_loop()` per thread. | [x] | Dev |
| TA.11 | Green | • **Achieve:** Wire async engine in composition root + native async health probe.<br>• **Deliver:** `src/ragent/bootstrap/composition.py` — `create_async_engine(aiomysql_dsn)`; `src/ragent/routers/health_probes.py` — `probe_mariadb` uses `async with engine.connect()`; `src/ragent/bootstrap/init_schema.py` — retains sync `pymysql` engine; `pyproject.toml` adds `aiomysql>=0.2.0` + `pytest-asyncio>=0.24` dev dep. | [x] | Dev |
| TA.12 | Refactor | • **Achieve:** Green tests stay green after structural cleanup.<br>• **Deliver:** `uv run ruff format . && uv run ruff check . --fix && uv run pytest --ignore=tests/e2e` all green; `test_async_repository_contract.py` and updated test files pass. | [x] | Reviewer |

---

## Phase 2 — Production Quality (+3 weeks) — *not started*

| # | Category | Task | Status | Owner |
|---|---|---|:---:|---|
| P2.1 | Stability | • **Achieve:** Production-grade HA + observability.<br>• **Deliver:** SRE HA verification report, monitoring dashboards, alerting rules. | [ ] | SRE |
| P2.2 | Security | • **Achieve:** Activate JWT + Permission layer per Track T8; B14 invariant (ES carries no auth fields).<br>• **Deliver:** All `[~]` rows in T8 → `[ ]` → `[x]`; remove `RAGENT_AUTH_DISABLED`; introduce `RAGENT_TRUST_X_USER_ID_HEADER` (default `false`) and per-surface `RAGENT_PERMISSION_INGEST_ENABLED` / `RAGENT_PERMISSION_CHAT_ENABLED` (both default `false`) — wiring lands but enforcement stays opt-in. | [ ] | Dev |
| P2.3 | Behavioral | • **Achieve:** Improve chat ranking via reranker.<br>• **Deliver:** `RerankClient` wired into chat pipeline as `HybridRetrieverWithRerank` SuperComponent. | [ ] | Dev |
| P2.4 | Behavioral | • **Achieve:** Route translate/summarize intents to direct LLM, bypassing retrieval.<br>• **Deliver:** `ConditionalRouter` intent split. | [ ] | Dev |
| P2.5 | Behavioral | • **Achieve:** Replace P1 501 stub with real MCP JSON-RPC 2.0 server exposing the `retrieve` tool (B47, §3.8).<br>• **Deliver:** decomposed into Track T-MCP rows; flips `[x]` when T-MCP.1–T-MCP.12 are all `[x]`. | [x] | Dev |
| P2.6 | Quality | • **Achieve:** Continuous answer-quality + load resilience evidence.<br>• **Deliver:** RAGAS eval in CI; large-file streaming; chaos drills (軌三 decomposed into Track T-CHAOS, B49). | [ ] | QA |
| P2.7 | Behavioral | • **Achieve:** Concurrent component execution for ingest/chat.<br>• **Deliver:** Switch ingest/chat to Haystack `AsyncPipeline`. | [ ] | Dev |
| P2.8 | Closure | • **Achieve:** Close P2 with synced docs and lessons.<br>• **Deliver:** Updated `00_spec.md` / `00_plan.md` + new entries in `00_journal.md`. | [ ] | Master |
| P2.9 | Stability | • **Achieve:** Bound MinIO orphan exposure to 24h.<br>• **Deliver:** Orphan MinIO sweeper with TTL 24h on staging objects (`event=minio.orphan_object` audit). | [ ] | SRE |

## Phase 3 — Graph Enhancement (conditional, +4–6 weeks) — *gated*

| # | Category | Task | Status | Owner |
|---|---|---|:---:|---|
| P3.1 | Decision | • **Achieve:** Lock graph DB choice with a written rationale.<br>• **Deliver:** ADR for Graph DB selection (Neo4j Community / ArcadeDB / Memgraph). | [ ] | Architect |
| P3.2 | Behavioral | • **Achieve:** Replace stub with a real graph extractor on the same Protocol.<br>• **Deliver:** `GraphExtractor` implementation replacing `StubGraphExtractor`. | [ ] | Dev |
| P3.3 | Behavioral | • **Achieve:** Add graph retrieval branch to chat pipeline.<br>• **Deliver:** `HybridRetrieverWithGraph` SuperComponent + `LightRAGRetriever` (200 ms TO → []). | [ ] | Dev |
| P3.4 | Governance | • **Achieve:** Govern entity lifecycle in the graph store.<br>• **Deliver:** Entity soft-delete + ref_count + GC + reconciliation cron. | [ ] | Dev |
| P3.5 | Gate | • **Achieve:** Confirm graph track is justified before spend.<br>• **Deliver:** Gate decision: P2 stable ≥ 4 weeks AND hybrid alone underperforms on relational queries. | [ ] | PM |

---

## Phase 1 v2 — Ingest API breaking-change refactor (2026-05-06)

> Source of truth: `docs/team/2026_05_06_ingest_api_v2.md` (6/6 PASS).
> Supersedes the v1 ingest stack (multipart upload, lang-aware chunker,
> chunks DB table, single-MinIO env). Six TIDY-FIRST commits C1–C6, each
> independently `make check` green.

### Tasks superseded by v2

The following v1 tasks are **superseded** (their `[x]` reflects v1 history;
v2 replaces the underlying behavior in C2–C6):

- T0.8 `chunks` DB table → **dropped**, ES is sole chunk store.
- T2.3 / T2.4 `ChunkRepository` → **deleted** in C6.
- T2.7 / T2.8 ingest-create multipart contract → **rewritten** by T2v.22–T2v.27 (C3).
- T2.13 / T2.14 multipart router → **replaced** by T2v.24–T2v.25 (C3).
- T3.1 / T3.2 lang-routed pipeline; T3.2k / T3.2l CSV row-merger → **replaced** by T2v.30–T2v.37 (C4).

### v2 task block (Track T2v)

| # | Category | Task | Commit | Status | Owner |
|---|---|---|:-:|:-:|---|
| T2v.20 | Structural | • **Achieve:** Add v2 documents columns + drop chunks table.<br>• **Deliver:** Alembic revision adding `ingest_type ENUM('inline','file')`, `minio_site VARCHAR(64) NULL`, `source_url VARCHAR(2048) NULL`; **DROP TABLE chunks**; schema-drift test green; `migrations/schema.sql` updated. | C2 | [x] | Dev |
| T2v.21 | Structural | • **Achieve:** Add ES `raw_content` field to `chunks_v1`.<br>• **Deliver:** `resources/es/chunks_v1.json` adds `"raw_content": {"type":"text","index":false,"doc_values":false}`; spec §5.2 sync; ES drift test green. | C2 | [x] | Dev |
| T2v.22 | Red  | • **Achieve:** Pin v2 request schema (discriminated union + validators).<br>• **Deliver:** `tests/unit/test_ingest_request_schema_v2.py` — `ingest_type` discriminator; `content_type ∈ {text/plain, text/markdown, text/html}` (415 outside set); `inline.content` non-empty UTF-8 string ≤ `INGEST_INLINE_MAX_BYTES`; `file.minio_site` validated against runtime registry; `source_url` opaque ≤ 2048; missing required → 422 with `errors[]`. | C3 | [x] | QA |
| T2v.23 | Green | • **Achieve:** Implement Pydantic discriminated request models.<br>• **Deliver:** `src/ragent/schemas/ingest.py` — `IngestMime` enum, `InlineIngestRequest`, `FileIngestRequest`, discriminated union with `discriminator="ingest_type"`. | C3 | [x] | Dev |
| T2v.24 | Red  | • **Achieve:** Pin v2 router contract (JSON only, no multipart).<br>• **Deliver:** `tests/unit/test_ingest_router_v2.py` — 202 happy path inline + file; 415 bad MIME; 413 inline overflow; 422 shape; old multipart `multipart/form-data` → 415. | C3 | [x] | QA |
| T2v.25 | Green | • **Achieve:** Implement v2 router (JSON-only).<br>• **Deliver:** `src/ragent/routers/ingest.py` — replace `UploadFile` handler with `IngestRequest` body; remove `python-multipart` import. **Breaking change.** | C3 | [x] | Dev |
| T2v.26 | Red  | • **Achieve:** Pin service `create` branching contract.<br>• **Deliver:** `tests/unit/test_ingest_service_v2.py` — inline → stage to `__default__` MinIO + `documents.ingest_type='inline'`; file → HEAD-probe caller's `(minio_site, object_key)`, no copy, `documents.ingest_type='file', minio_site=...`; HEAD miss → 422 `INGEST_OBJECT_NOT_FOUND`; readonly site refuses post-READY delete. | C3 | [x] | QA |
| T2v.27 | Green | • **Achieve:** Implement branched create + structured business log.<br>• **Deliver:** `src/ragent/services/ingest_service.py::create` — discriminated dispatch; emit `event=ingest.received {document_id, ingest_type, content_type, source_id, source_app}`. | C3 | [x] | Dev |
| T2v.28 | Red  | • **Achieve:** Pin `MinioSiteRegistry` semantics.<br>• **Deliver:** `tests/unit/test_minio_site_registry.py` — boot fails fast on missing `__default__`; per-site client cached; `get(name)` raises `UnknownMinioSite` on miss; `read_only` flag exposed; HEAD-probe helper (`stat_object`) returns size. | C3 | [x] | QA |
| T2v.29 | Green | • **Achieve:** Implement registry + composition wiring.<br>• **Deliver:** `src/ragent/storage/minio_registry.py` (parses `MINIO_SITES` JSON env) + `bootstrap/composition.py` adds `MinioSiteRegistry` singleton. | C3 | [x] | Dev |
| T2v.30 | Red  | • **Achieve:** Pin `_TextLoader` Haystack component.<br>• **Deliver:** `tests/unit/test_text_loader.py` — accepts inline `str` or worker-fetched `bytes`+`mime_type`; returns `Document(content=str, meta={mime_type, document_id, source_url, source_title, source_app, source_workspace})`. | C4 | [x] | QA |
| T2v.31 | Green | • **Achieve:** Implement `_TextLoader` (~10 LOC).<br>• **Deliver:** `src/ragent/pipelines/factory.py::_TextLoader`. | C4 | [x] | Dev |
| T2v.32 | Red  | • **Achieve:** Pin `_MarkdownASTSplitter` (mistletoe).<br>• **Deliver:** `tests/unit/test_markdown_ast_splitter.py` — atoms: heading-section, fenced code, list, table, blockquote, paragraph; **never split inside** a fenced code block; `meta["raw_content"]` round-trips byte-for-byte; deterministic across re-runs. | C4 | [x] | QA |
| T2v.33 | Green | • **Achieve:** Implement `_MarkdownASTSplitter` via mistletoe AST walk.<br>• **Deliver:** `src/ragent/pipelines/factory.py::_MarkdownASTSplitter`. | C4 | [x] | Dev |
| T2v.34 | Red  | • **Achieve:** Pin `_HtmlASTSplitter` (selectolax).<br>• **Deliver:** `tests/unit/test_html_ast_splitter.py` — drops `<script>/<style>/<nav>/<aside>/<footer>/<header>` (when not nested in `<article>/<main>`); atoms: heading-section, `<pre>`, `<table>`, `<article>` paragraphs; `meta["raw_content"]` is well-formed serialized HTML fragment; deterministic. | C4 | [x] | QA |
| T2v.35 | Green | • **Achieve:** Implement `_HtmlASTSplitter` via selectolax DOM walk.<br>• **Deliver:** `src/ragent/pipelines/factory.py::_HtmlASTSplitter`. | C4 | [x] | Dev |
| T2v.36 | Red  | • **Achieve:** Pin `_BudgetChunker` (mime-agnostic, 1000/1500/100).<br>• **Deliver:** `tests/unit/test_budget_chunker.py` — greedy-pack atoms ≤ `CHUNK_TARGET_CHARS` (1000); atoms > `CHUNK_MAX_CHARS` (1500) hard-split with `CHUNK_OVERLAP_CHARS` (100); concatenated `meta["raw_content"]` matches union of source atom raws. | C4 | [x] | QA |
| T2v.37 | Green | • **Achieve:** Implement `_BudgetChunker`.<br>• **Deliver:** `src/ragent/pipelines/factory.py::_BudgetChunker` (replaces `_CharBudgetChunker`). | C4 | [x] | Dev |
| T2v.38 | Red  | • **Achieve:** Pin `FileTypeRouter` wiring + unroutable failure path.<br>• **Deliver:** `tests/unit/test_pipeline_routing_v2.py` — each MIME routes to its splitter; unknown MIME → `_RaiseUnroutable` → worker FAILED with `error_code=PIPELINE_UNROUTABLE` (mirrors PIPELINE_TIMEOUT). | C4 | [x] | QA |
| T2v.39 | Green | • **Achieve:** Wire pipeline graph: `_TextLoader → FileTypeRouter → {DocumentSplitter \| _MarkdownASTSplitter \| _HtmlASTSplitter} → DocumentJoiner → _IdempotencyClean → _BudgetChunker → DocumentEmbedder → DocumentWriter(ES only)`. **DocumentWriter writes to ES only — no chunks DB.** | C4 | [x] | Dev |
| T2v.40 | Red  | • **Achieve:** Pin chat read-path uses `raw_content` with `content` fallback.<br>• **Deliver:** `tests/unit/test_chat_uses_raw_content.py` — LLM-context builder + `_ExcerptTruncator` use `doc.meta.get("raw_content") or doc.content`; reranker still scores on `content`; `sources[].source_url` populated. | C5 | [x] | QA |
| T2v.41 | Green | • **Achieve:** Implement chat read-path + `source_url` in citations.<br>• **Deliver:** `src/ragent/pipelines/chat.py` updates; `routers/retrieve.py` exposes `source_url` in chunk envelope. | C5 | [x] | Dev |
| T2v.42 | Red  | • **Achieve:** Pin per-step business + failure logs (Logging Rule extension).<br>• **Deliver:** `tests/unit/test_pipeline_logging.py` — each pipeline component (`_TextLoader`, splitter, `_BudgetChunker`, embedder, writer) emits `event=ingest.step.{started,ok,failed}` carrying `{document_id, step, mime_type, atoms_in?, chunks_out?, duration_ms, error_code?, error?}`. Failures map to `PIPELINE_UNROUTABLE` / `EMBEDDER_ERROR` / `ES_WRITE_ERROR` / `PIPELINE_TIMEOUT`; happy-path emits `event=ingest.ready` with chunk count. | C5 | [x] | QA |
| T2v.43 | Green | • **Achieve:** Wire structlog per-step events + correlate via OTEL.<br>• **Deliver:** Each component logs via `structlog.get_logger("ragent.ingest")` with `document_id` bound; reuse existing OTEL span context (T7.1b). | C5 | [x] | Dev |
| T2v.44 | Refactor | • **Achieve:** Delete dead v1 code.<br>• **Deliver:** Remove `_CharBudgetChunker` lang branches, CJK constants, `langdetect`/`nltk` deps, `python-multipart` dep, `ChunkRepository`, multipart imports, T3.2l RowMerger, v1 chunker env vars. | C6 | [x] | Dev |
| T2v.45 | Acceptance | • **Achieve:** Golden end-to-end test with wiremock embedding + testcontainers.<br>• **Deliver:** `tests/e2e/test_ingest_v2_golden.py` — fixtures spin up MariaDB + Redis + ES + MinIO via testcontainers; `pytest-httpserver` mocks Embedding API; for each `(ingest_type, mime) ∈ {inline, file} × {text/plain, text/markdown, text/html}` (6 combos): POST → broker → real worker → real pipeline → assert (a) ES `chunks_v1` rows exist with both `content` (normalized) and `raw_content` (original); (b) MariaDB `documents` row reaches `READY` with correct `ingest_type`/`minio_site`/`source_url`/`source_title`; (c) **no `chunks` table reads** (table dropped); (d) per-step structured-log events present in captured stream; (e) markdown fenced code survives in `raw_content`, HTML `<script>/<nav>` excluded from both fields. | C6 | [x] | QA |

### Source-id review follow-up (Track T-SR, branch `claude/review-source-id-handling-dq6nj`)

| # | Category | Task | Commit | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-SR.1 | Behavioral | • **Achieve:** Cascade ES chunk delete when supersede picks a loser (spec §3.1 was leaking orphan chunks).<br>• **Deliver:** `src/ragent/services/ingest_service.py::supersede` routes losers through `self.delete()` so `claim_for_deletion → fan_out_delete → repo.delete` runs end-to-end; `tests/integration/test_supersede_task.py` asserts `broker.fan_out_delete` once per loser. | 13a2e28 | [x] | Dev |
| T-SR.2 | Structural | • **Achieve:** Capture the surrogate-PK + biz-UNIQUE rule and lock the revision-model decisions.<br>• **Deliver:** New `Database Practices > Mandatory Surrogate PK + Business Unique Key` rule in `docs/00_rule.md`; `docs/team/2026_05_07_revision_model_proposal.md`; spec §7 rows B32 / B33 / B34. | 934a478 / pending | [x] | Architect |
| T-SR.3 | Structural | • **Achieve:** Rename `documents.source_workspace` → `source_meta` and widen to `VARCHAR(1024)` (free-format metadata).<br>• **Deliver:** Migration `migrations/005_rename_source_workspace_to_source_meta.sql` (`ALGORITHM=COPY, LOCK=SHARED`); `migrations/schema.sql`, `resources/es/chunks_v1.json` (with `ignore_above: 1024`); end-to-end rename in `src/ragent/{schemas,repositories,services,workers,plugins,pipelines,routers}` + all tests; `SOURCE_META_MAX = 1024` in `schemas/ingest.py` is the single source for the cap (imported by `schemas/chat.py`, `routers/retrieve.py`); spec §7 row B35; spec §3.1/§3.4/§5.1/§5.2 prose; `docs/API.md`. | pending | [x] | Dev |
| T-SR.4 | Behavioral | • **Achieve:** DB-side survivor election in `pop_oldest_loser_for_supersede` so an out-of-order finish can never delete a strictly-newer row.<br>• **Deliver:** `src/ragent/repositories/document_repository.py` SQL JOIN-on-newest (inner subquery picks `ORDER BY created_at DESC LIMIT 1`; outer pop runs only when the caller's `survivor_id` matches that elected row); `tests/unit/test_document_repository.py::test_pop_oldest_loser_query_elects_survivor_via_db_max_created_at`. | pending | [x] | Dev |
| T-SR.5 | Behavioral | • **Achieve:** Hydration surfaces only `READY` rows so chat never cites mid-flight or DELETING docs.<br>• **Deliver:** `src/ragent/repositories/document_repository.py::get_sources_by_document_ids` adds `AND status = 'READY'`; `tests/unit/test_document_repository.py::test_get_sources_by_document_ids_filters_to_status_ready`. | pending | [x] | Dev |
| T-SR.6 | Structural | • **Achieve:** Auto-create configured MinIO bucket(s) at boot so `POST /ingest` does not 500 on a fresh install.<br>• **Deliver:** `src/ragent/bootstrap/init_schema.py::init_minio_buckets` wired into `auto_init`; idempotent `bucket_exists` → `make_bucket`; `tests/unit/test_init_schema.py` covers create / skip-when-exists / unconfigured-skip / propagated-error branches; `tests/unit/test_minio_site_registry.py` adds coverage for `delete_object` read-only + `NoSuchKey` swallow + non-404 propagation paths. | pending | [x] | Dev |
| T-SR.7 | Structural | • **Achieve:** Split test tiers so the pre-commit gate runs unit + integration only (e2e is its own tier — slow, container-orchestration heavy, flake-prone, run as a release step).<br>• **Deliver:** `Makefile` exposes `make test-gate` (excludes `tests/e2e`) for the pre-commit gate; `make test` continues to run the full suite. | 2de1408 (main) | [x] | Dev |

### ICU analyzer convergence (Track T-ICU, branch `claude/investigate-haystack-icu-analysis-bCaGr`)

| # | Category | Task | Commit | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-ICU.1 | Structural | • **Achieve:** Reconcile spec §5.2 with B26 (prose, JSON block, Decision Log B42).<br>• **Deliver:** spec §5.2 prose (BM25 analyzer paragraph rewritten to ICU + test override), §5.2 JSON block (adds `analysis.analyzer.icu_text` and `analyzer: icu_text` on `text`/`title`), §7 row B42 (test override rationale + alternatives). | eb7480a | [x] | Dev |
| T-ICU.2 | Red | • **Achieve:** Pin ICU analyzer in prod mapping; pin standard analyzer in test mapping; pin `init_es` env override; pin test↔prod structural equality (minus ICU deltas).<br>• **Deliver:** `tests/unit/test_init_schema.py` adds: (a) prod `resources/es/chunks_v1.json` includes `icu_text` analyzer + `text`/`title` reference it; (b) `tests/resources/es/chunks_v1.json` exists, no `analysis` block, `text`/`title` carry no analyzer (default standard); (c) test↔prod equal after stripping ICU deltas; (d) `init_es` reads `RAGENT_ES_RESOURCES_DIR` env when set; falls back to `_ES_RESOURCES`. | 1cc791d | [x] | QA |
| T-ICU.3 | Green | • **Achieve:** Implement env-driven mapping dir + commit two mapping files.<br>• **Deliver:** `resources/es/chunks_v1.json` (ICU); `tests/resources/es/chunks_v1.json` (standard); `src/ragent/bootstrap/init_schema.py::init_es` reads `RAGENT_ES_RESOURCES_DIR`; `tests/conftest.py` sets the env at session scope to point at the test resources dir. | 1cc791d | [x] | Dev |
| T-ICU.4 | Acceptance | • **Achieve:** Manual / staging smoke test for CJK BM25 (S36 coverage gap from B42).<br>• **Deliver:** Documented procedure: operator runs `Dockerfile.es-test` ES, applies prod mapping, indexes a `"產品規格"` doc, verifies `_analyze` tokenises into `["產品", "規格"]` and BM25 query recalls. Tracked as a release-gate manual step; not blocking pre-commit. | T-ICU.3 | [ ] | Ops |

### Reconciler-as-safety-net follow-up (Track T-RR, branch `claude/add-app-doctor-wPAZH`) — 2026-05-08

> Source: in-session audit `2026-05-08`. Five-fix track (F1–F5) decoupling
> retrieval correctness from background cleanup completeness so the
> reconciler is **only** a safety net for crash recovery + disk reclaim,
> not load-bearing for user-visible correctness.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-RR.1 | Red | • **Achieve:** Pin `_SourceHydrator` drop-on-miss semantics (B36 / S6j).<br>• **Deliver:** `tests/unit/test_source_hydrator_drop_on_miss.py` — chunk whose `document_id` is absent from `get_sources_by_document_ids` result is **not** in `result["documents"]`; chunk whose `document_id` is present is enriched and kept; mixed batch → only matched docs survive. | B36 | [x] | QA |
| T-RR.2 | Green | • **Achieve:** Implement drop-on-miss in hydrator.<br>• **Deliver:** `src/ragent/pipelines/chat.py::_SourceHydrator.run` — replace pass-through `else: result.append(doc)` with skip; existing match branch unchanged. | B36 | [x] | Dev |
| T-RR.3 | Refactor | • **Achieve:** Update existing chat-pipeline tests that conflated retrieval verification with pass-through hydration.<br>• **Deliver:** `tests/integration/test_chat_pipeline_retrieval.py` — tests asserting "retrieval recalls a doc" must mock `doc_repo.get_sources_by_document_ids` to include the doc; pipeline-level "hydrator passes through on miss" assertions removed (contract changed by B36). | B36 | [x] | Dev |
| T-RR.4 | Red | • **Achieve:** Pin composition no longer requires legacy `MINIO_ENDPOINT` / `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY` when `MINIO_SITES` is set.<br>• **Deliver:** `tests/unit/test_composition_minio_wiring.py` — with `MINIO_SITES` JSON set and legacy three vars unset, `build_container()` succeeds; `/readyz` minio probe uses `minio_registry.default().client`. | B37 | [x] | QA |
| T-RR.5 | Structural | • **Achieve:** Switch `/readyz` minio probe source from legacy `MinIOClient` to registry default site.<br>• **Deliver:** `src/ragent/bootstrap/app.py::_build_probes` reads `container.minio_registry.default().client`. | B37 | [x] | Dev |
| T-RR.6 | Green | • **Achieve:** Remove unconditional `_require` of legacy MinIO vars when `MINIO_SITES` is set.<br>• **Deliver:** `src/ragent/bootstrap/composition.py` — guard the legacy `MinIOClient` construction (or drop it entirely if no consumer remains). | B37 | [x] | Dev |
| T-RR.7 | Red | • **Achieve:** Pin AI token boot-time pre-warm — wrong `AI_API_AUTH_URL` or stale J1 fails boot, not first request.<br>• **Deliver:** `tests/unit/test_check_infra_ready_token_warm.py` — `_check_infra_ready` invokes `tm.get_token()` for each token manager; failure raises `RuntimeError`. | B38 | [x] | QA |
| T-RR.8 | Green | • **Achieve:** Pre-warm tokens in lifespan startup.<br>• **Deliver:** `src/ragent/bootstrap/app.py::_check_infra_ready` — for each `tm` in `container.token_managers`, run `await anyio.to_thread.run_sync(tm.get_token)` so token-exchange failure surfaces before `/readyz` succeeds. | B38 | [x] | Dev |
| T-RR.9 | Red | • **Achieve:** Pin worker's atomic promote-and-demote on READY (B39 / supersede race window closure).<br>• **Deliver:** `tests/integration/test_worker_atomic_promote.py` — worker finishes doc B → in same tx, B becomes READY and any other `(source_id, source_app)` row with `status='READY'` becomes `DELETING`; combined with B36 hydrator drop, `/chat` retrieves only B's chunks immediately. | B39 | [x] | QA |
| T-RR.10 | Green | • **Achieve:** Implement atomic promote-demote in repository.<br>• **Deliver:** `src/ragent/repositories/document_repository.py::promote_to_ready_and_demote_siblings(document_id, source_id, source_app)` — single tx: `UPDATE … SET status='READY' WHERE document_id=:id AND status='PENDING'` then `UPDATE … SET status='DELETING' WHERE source_id=:src AND source_app=:app AND document_id != :id AND status='READY'`. Worker's READY transition switches to this method. | B39 | [x] | Dev |
| T-RR.11 | Red | • **Achieve:** Pin HTTP `DELETE /ingest/{id}` actually runs `fan_out_delete` (closes B3 cascade gap).<br>• **Deliver:** `tests/integration/test_http_delete_runs_fan_out.py` — DELETE on a READY doc → `PluginRegistry.fan_out_delete(document_id)` invoked once → ES chunks for that doc absent post-DELETE; row hard-deleted; 204. | B40 | [x] | QA |
| T-RR.12 | Structural | • **Achieve:** Inject `PluginRegistry` into `IngestService` so HTTP DELETE can call `fan_out_delete`.<br>• **Deliver:** `src/ragent/services/ingest_service.py` constructor takes a `registry` arg; `bootstrap/app.py::create_app` wires `container.registry` into `IngestService(...)`. | B40 | [x] | Dev |
| T-RR.13 | Green | • **Achieve:** Replace `_has_fan_out` introspection with explicit registry call.<br>• **Deliver:** `src/ragent/services/ingest_service.py::delete` — `await registry.fan_out_delete(document_id)` between status-DELETING and row-delete; remove `_has_fan_out` flag and broker introspection. | B40 | [x] | Dev |
| T-RR.14 | Red | • **Achieve:** Pin worker promote is DB-arbitrated by `MAX(created_at)` so out-of-order worker completion never leaves an older revision READY. Closes the residual race that previously left the reconciler load-bearing for retrieval correctness.<br>• **Deliver:** `tests/integration/test_worker_atomic_promote.py::test_older_worker_finishing_after_newer_pending_self_demotes` and `::test_older_worker_finishing_after_newer_ready_self_demotes`. | B41 | [x] | QA |
| T-RR.15 | Green | • **Achieve:** Implement DB-side survivor election in worker promote; non-survivors self-demote so the newer worker's tx is the one that flips retrieval.<br>• **Deliver:** `src/ragent/repositories/document_repository.py::promote_to_ready_and_demote_siblings` — `SELECT … FROM documents WHERE source_id/app, status IN (PENDING,READY) ORDER BY created_at DESC LIMIT 1 FOR UPDATE` elects survivor; survivor branch promotes + demotes prior READY, non-survivor branch flips own PENDING → DELETING. | B41 | [x] | Dev |
| T-RR.16 | Red | • **Achieve:** Pin that post-READY enrichment (`fan_out`) does NOT run when the worker self-demotes, so READY-only plugins (event publish, cache warm) never see a non-READY doc.<br>• **Deliver:** `tests/integration/test_worker_minio_cleanup.py::test_self_demote_skips_post_ready_fan_out`. | B41 | [x] | QA |
| T-RR.17 | Green | • **Achieve:** Gate worker `fan_out` on promote outcome.<br>• **Deliver:** `promote_to_ready_and_demote_siblings` returns `bool` (True survivor / False self-demoted); `src/ragent/workers/ingest.py` reads return value and skips `container.registry.fan_out(document_id)` when False. Existing test mocks updated to return `True`. Spec row B41 added in `docs/00_spec.md`. | B41 | [x] | Dev |
| T-RR.18 | Red | • **Achieve:** Pin the `FOR UPDATE` lock semantic — a second concurrent promote on the same `(source_id, source_app)` must block on the row lock until the first tx commits, so retrieval correctness is enforced by the DB, not by application-level serialization.<br>• **Deliver:** `tests/integration/test_worker_atomic_promote.py::test_for_update_serializes_concurrent_promotes` — holds the survivor `SELECT … FOR UPDATE` from outside the repo, asserts a parallel promote is blocked via `wait_for(shield(task), timeout=0.1)` raising `TimeoutError`, then releases and verifies the elected survivor wins. | B41 | [x] | QA |


### Retrieve + Ingest API enhancements (branch `claude/add-ingest-filters-EIXOq`) — 2026-05-11

> Source: user request 2026-05-11. Three paired behavioural additions to the retrieve and ingest surfaces.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-EF.1 | Behavioral | • **Achieve:** Add `top_k`/`min_score` request params to `POST /retrieve`; expose `source_meta` in chunk response; add Pydantic `response_model=` to all endpoints.<br>• **Deliver:** `src/ragent/routers/retrieve.py` (`ChunkEntry`, `RetrieveResponse`, `top_k`/`min_score` fields); `src/ragent/pipelines/chat.py` (`source_meta` in `doc_to_source_entry`, `top_k`/`min_score` in `run_retrieval`, `_retriever_params` helper); `src/ragent/routers/ingest.py` (four response models, `response_model=` on all routes); `tests/unit/test_retrieve_router.py`. | B7/B35 | [x] | Dev |
| T-EF.2 | Behavioral | • **Achieve:** Add `source_id`/`source_app` filter params to `GET /ingest` list; change list ordering to newest-first (`document_id DESC`).<br>• **Deliver:** `src/ragent/repositories/document_repository.py` (`list()` adds filter params + DESC order + `< :after` cursor); `src/ragent/services/ingest_service.py` (pass-through); `src/ragent/routers/ingest.py` (query params); `tests/unit/test_document_repository.py` + `test_ingest_service_list.py` + `test_ingest_router_v2.py`. | B7 | [x] | Dev |
| T-EF.3 | Behavioral | • **Achieve:** Fix `min_score` implementation — `score_threshold` is not a valid ES retriever `run()` param; apply as post-retrieval filter instead.<br>• **Deliver:** `src/ragent/pipelines/chat.py` (`_retriever_params` drops `score_threshold`; `run_retrieval` filters by score after `pipeline.run()`); four new tests in `tests/unit/test_retrieve_router.py`. | B7 | [x] | Dev |

### top_k hard cap fix (branch `claude/fix-top-k-cap`) — 2026-05-11

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-EF.4 | Behavioral | • **Achieve:** Enforce `top_k` as a hard post-pipeline cap in `run_retrieval()` — Haystack's `DocumentJoiner` and `_Reranker` use their init `top_k` and may return more results than the per-request value.<br>• **Deliver:** `src/ragent/pipelines/chat.py` (`docs = docs[:top_k]` after pipeline output); `tests/unit/test_retrieve_router.py` (three new tests: hard cap, cap after min_score, top_k=None returns all). | B7 | [x] | Dev |

### score field in retrieve response (branch `claude/fix-top-k-cap`) — 2026-05-11

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-EF.5 | Behavioral | • **Achieve:** Expose retrieval score in `POST /retrieve` chunk response.<br>• **Deliver:** `score: float | None` added to `ChunkEntry` in `routers/retrieve.py`; `doc_to_source_entry()` in `pipelines/chat.py` includes `"score": doc.score`; two new tests (score present, score=None). | §3.4.4 | [x] | Dev |

### API path versioning (branch `claude/add-api-versioning-7jm6C`) — 2026-05-11

> Source: user request 2026-05-11. All business API paths must carry a `/v1` version segment.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-AV.1 | Behavioral | • **Achieve:** Add `/v1` version segment to all business API paths: `POST/GET/DELETE /ingest/v1[/{id}]`, `POST /chat/v1[/stream]`, `POST /retrieve/v1`, `POST /mcp/v1/tools/rag`.<br>• **Deliver:** `src/ragent/routers/{ingest,chat,retrieve,mcp}.py` — routers gain `prefix="/ingest/v1"` / `/chat/v1` / `/retrieve/v1` / `/mcp/v1`; route decorators drop the resource segment. All tests updated to new paths. Spec §3.4 / §4.1.2 path table updated. | §3.4 | [x] | Dev |

## Binary Document Loaders (DOCX / PPTX)

| # | Category | Task | Spec | Status | Owner |
|---|---|:-:|:-:|:-:|---|
| T-BL.1 | Red | • **Achieve:** Pin `_DocxASTSplitter` atom contract.<br>• **Deliver:** `tests/unit/test_docx_ast_splitter.py` — heading/paragraph/table atoms, blank paragraphs skipped, meta passthrough, empty doc → zero atoms, table raw_content is Markdown. | §4.2 | [x] | QA |
| T-BL.2 | Red | • **Achieve:** Pin `_PptxASTSplitter` atom contract.<br>• **Deliver:** `tests/unit/test_pptx_ast_splitter.py` — one atom per slide, blank slide skipped, slide_number in meta, meta passthrough, empty presentation → zero atoms. | §4.2 | [x] | QA |
| T-BL.3 | Green | • **Achieve:** Implement `_DocxASTSplitter` and `_PptxASTSplitter`.<br>• **Deliver:** `src/ragent/pipelines/factory.py` — both components; `IngestMime.DOCX`/`IngestMime.PPTX` enum values; `BINARY_MIMES` frozenset derived from `IngestMime`; `_TextLoader` accepts `content_bytes`; `_MimeAwareSplitter` routes via `IngestMime` enum constants; worker `BINARY_MIMES` branch skips UTF-8 decode; `python-docx`/`python-pptx` added as deps. | §4.2 | [x] | Dev |
| T-BL.4 | Acceptance | • **Achieve:** `_MimeAwareSplitter` dispatch covers all new routes.<br>• **Deliver:** `tests/unit/test_pipeline_routing_v2.py` — DOCX and PPTX routes exercise the full dispatch path end-to-end. | §4.2 | [x] | QA |

### DOCX/PPTX splitter review fixes (PR #54) — 2026-05-11

| # | Category | Task | Spec | Status | Owner |
|---|---|:-:|:-:|:-:|---|
| T-BL.5 | Structural | • **Achieve:** Address Gemini/Codex PR review findings.<br>• **Deliver:** `_DocxASTSplitter`/`_PptxASTSplitter` strip `raw_bytes` from atom meta (`base_meta`); `_PptxASTSplitter` extracts table cell text; `_table_to_markdown` escapes `|` and normalises newlines in cell text; `_PptxASTSplitter` uses `para.text` instead of manual run join. | §4.2 | [x] | Dev |

### PPTX mime_type UX + pipeline error fix — 2026-05-12

| # | Category | Task | Spec | Status | Owner |
|---|---|:-:|:-:|:-:|---|
| T-BL.6 | Behavioral | • **Achieve:** Accept short aliases `pptx`/`docx` at all API entry points.<br>• **Deliver:** `IngestMime._missing_` classmethod + module-level `_MIME_ALIASES` constant; `BINARY_MIMES` moved to `schemas/ingest.py` as canonical source; `factory.py` and `workers/ingest.py` import from there. Alias coverage: JSON body, Form field (upload endpoint), case-insensitive. | §4.1 | [x] | Dev |
| T-BL.7 | Behavioral | • **Achieve:** Reject binary MIME (DOCX/PPTX) on `ingest_type=inline` at schema validation time.<br>• **Deliver:** `InlineIngestRequest.model_validator(mode="after")` raises 422 `INGEST_VALIDATION` when `mime_type in BINARY_MIMES`; clears "file is not a zip file" pipeline crash for inline callers. | §4.1 | [x] | Dev |
| T-BL.8 | Behavioral | • **Achieve:** Worker uses `doc.mime_type` (DB) as authoritative MIME routing key, not MinIO content-type.<br>• **Deliver:** `workers/ingest.py` — `mime = doc.mime_type or minio_content_type or DEFAULT_MIME`; fixes "file is not a zip file" for `ingest_type=file` when caller's MinIO object has generic/missing content-type. | §4.1 | [x] | Dev |
| T-BL.9 | Behavioral | • **Achieve:** Case-insensitive MIME handling per RFC 2045 §5.1 (PR #58 review).<br>• **Deliver:** `IngestMime._missing_` accepts full MIME strings with mixed casing (e.g., `TEXT/PLAIN`) via lowercase-enum-value fallback after the alias dict; worker lowercases the resolved mime so MinIO metadata with non-standard casing (e.g., `APPLICATION/OCTET-STREAM`) routes correctly. | §4.1 | [x] | Dev |
| T-BL.10 | Behavioral | • **Achieve:** Fix `mime_type=None` in all `ingest.step.*` structured logs for PPTX/DOCX uploads.<br>• **Deliver:** `workers/ingest.py` — pass `mime_type=doc.mime_type` to `bind_ingest_context`; `tests/unit/test_ingest_worker_mime_routing.py` — `test_worker_binds_doc_mime_type_to_structlog_context` pins the call. | §T2v.42 | [x] | Dev |
| T-BL.11 | Behavioral | • **Achieve:** Log `file_size_bytes` in the load step and `splitter` name in the split step of `ingest.step.ok` events.<br>• **Deliver:** `pipelines/observability.py` — `_ctx()` returns all bound contextvars; `ingest.step.ok` refreshes ctx after component runs; `workers/ingest.py` — bind `file_size_bytes=len(data)`; `pipelines/factory.py` — `_MimeAwareSplitter` binds `splitter=<name>`; `tests/unit/test_pipeline_logging.py` — two new tests pin these. | §T2v.42 | [x] | Dev |
| T-BL.12 | Behavioral | • **Achieve:** Ensure `mime_type` appears in all `ingest.step.*` logs for legacy rows where `doc.mime_type=NULL`.<br>• **Deliver:** `workers/ingest.py` — also bind `mime_type=mime` (resolved value) alongside `file_size_bytes` so the step-log context carries the authoritative MIME even when `bind_ingest_context` skips it. | §T2v.42 | [x] | Dev |


### Ingest pipeline file-type bug fixes — 2026-05-12

| # | Category | Task | Spec | Status | Owner |
|---|---|:-:|:-:|:-:|---|
| T-FIL.1 | Behavioral | • **Achieve:** Fix `head_object` `or 0` bug that caused `OSError: MinIO get_object size mismatch` for any file whose `stat.size` is `None`.<br>• **Deliver:** `storage/minio_registry.py` — remove `or 0` from `size = getattr(stat, "size", None)`; update return type to `tuple[int \| None, str \| None] \| None`; regression tests in `tests/unit/test_minio_site_registry.py`. | §3.1 | [x] | Dev |
| T-FIL.2 | Behavioral | • **Achieve:** Enforce `INGEST_FILE_MAX_BYTES` limit for `ingest_type=file` ingests (spec §4.2).<br>• **Deliver:** `services/ingest_service.py` — `_record_file` checks `stat_object` size against limit and raises `FileTooLarge`; `create()` accepts `max_file_bytes` parameter; test `test_file_too_large_raises_413` in `tests/unit/test_ingest_service_v2.py`. | §4.2 | [x] | Dev |
| T-FIL.3 | Behavioral | • **Achieve:** Replace `SELECT … FOR UPDATE` with lock-free atomic correlated-subquery UPDATE in `promote_to_ready_and_demote_siblings`.<br>• **Deliver:** `repositories/document_repository.py` — election uses MVCC subquery wrapped in derived table (MariaDB restriction), outer UPDATE locks only the single target row; no blocking between concurrent workers; existing `idx_source_app_id_status_created` index used; docker integration tests updated to verify correct survivor election. | §B39 | [x] | Dev |
| T-FIL.4 | Behavioral | • **Achieve:** Verify `ingest_type=file` worker never calls `delete_object` (caller owns the bytes).<br>• **Deliver:** `tests/integration/test_worker_minio_cleanup.py` — `test_file_type_ingest_does_not_delete_minio_object` added; removed incorrect `pytestmark = pytest.mark.docker` (mock-only tests do not require Docker). | §3.1 | [x] | Dev |
| T-FIL.5 | Behavioral | • **Achieve:** Fix `_record_file` false `ObjectNotFoundError` for files with unknown size metadata.<br>• **Deliver:** `services/ingest_service.py` — switch from `stat_object` (returns `None` for both missing AND size=None) to `head_object` (returns `None` only for missing; `(None, ct)` for exists-with-unknown-size); size limit check guarded with `size is not None`; test mock updated to model `head_object` semantics. | §3.1 | [x] | Dev |
| T-FIL.6 | Behavioral | • **Achieve:** Guard `_log_transition("PENDING", "DELETING")` on actual row change — phantom audit entries when row is already non-PENDING.<br>• **Deliver:** `repositories/document_repository.py` — capture `demoted.rowcount` from self-demotion UPDATE; only call `_log_transition` when `rowcount == 1`. | §B39 | [x] | Dev |

### Unprotect API integration (branch `claude/add-unprotect-api-integration-coBow`) — 2026-05-13

> Source: user request 2026-05-13. Before the worker feeds bytes to the ingest pipeline, optionally call an external unprotect API to obtain the raw (decrypted/unDRM'd) binary.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-UP.1 | Red | • **Achieve:** Pin `UnprotectClient` contract: multipart POST with `fileInput` + `delegatedUser`, `apikey` header, returns raw bytes; delegates HTTP errors to caller.<br>• **Deliver:** `tests/unit/test_unprotect_client.py` — happy path returns response bytes; `delegatedUser` is `user_id + suffix`; HTTP error raises `httpx.HTTPStatusError`. | §4.6.4 | [x] | QA |
| T-UP.2 | Red | • **Achieve:** Pin worker unprotect-gate behaviour: when `container.unprotect_client` is set, bytes flowing into the pipeline are those returned by the unprotect API; when unset, original MinIO bytes pass through unchanged.<br>• **Deliver:** `tests/unit/test_ingest_worker_unprotect.py` — enabled path: `unprotect_client.unprotect` called with MinIO bytes + `doc.create_user`; pipeline receives unprotected bytes. Disabled path: `unprotect_client` is `None`, pipeline receives original bytes unchanged. | §4.6.4 | [x] | QA |
| T-UP.3 | Green | • **Achieve:** Implement `UnprotectClient` and wire it into the composition root and worker.<br>• **Deliver:** `src/ragent/clients/unprotect.py` — synchronous `unprotect(file_bytes, user_id, filename)` method; reads `UNPROTECT_API_URL`, `UNPROTECT_APIKEY`, `UNPROTECT_DELEGATED_USER_SUFFIX`; uses shared `httpx.Client`. `src/ragent/bootstrap/composition.py` — `Container.unprotect_client`; `build_container` checks `UNPROTECT_ENABLED`. `src/ragent/workers/ingest.py` — after `get_object`, if `container.unprotect_client` is not None, replace `data` with unprotect result. `.env.example` updated with four new vars in §4.6.4. | §4.6.4 | [x] | Dev |

### PDF ingest support (branch `claude/add-pdf-ingest-support-bghaI`) — 2026-05-13

> Source: user request 2026-05-13. Onboard `application/pdf` as a new ingest MIME type using PyMuPDF (pymupdf4llm / fitz). One atom per page; fast MuPDF path for text-only pages; Tesseract OCR for image-bearing pages; Dockerfile with Tesseract language packs.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-PDF.1 | Red | • **Achieve:** Pin `_PdfASTSplitter` atom contract: one atom per page, blank pages skipped, `page_number` 1-based, meta passthrough, `raw_content` set, batch mode processes all pages.<br>• **Deliver:** `tests/unit/test_pdf_ast_splitter.py` — 10 tests covering page atoms, content, raw_content, meta passthrough, empty-page skip, page_number, fast text path (no images), OCR success, OCR fallback, batch mode. | §4.2 | [x] | QA |
| T-PDF.2 | Green | • **Achieve:** Implement `_PdfASTSplitter` and helper `_pdf_page_text`; add Dockerfile with Tesseract (eng/chi_sim/chi_tra/jpn/deu); Windows install docs in README.<br>• **Deliver:** `src/ragent/pipelines/factory.py` — `PDF_PAGE_BATCH_THRESHOLD`, `PDF_BYTES_BATCH_THRESHOLD`, `PDF_BATCH_PAGES`, `PDF_OCR_LANGUAGES` constants; `_pdf_page_text(page)` fast/OCR/fallback helper; `@component _PdfASTSplitter` (NOT yet wired into `_MimeAwareSplitter`). `pyproject.toml` — `pymupdf4llm>=0.0.17` (runtime), `fpdf2>=2.0` (dev). `Dockerfile` — new file with Tesseract packs. `README.md` — Windows Tesseract setup. | §4.2 | [x] | Dev |
| T-PDF.3 | Green | • **Achieve:** Wire `application/pdf` end-to-end through schema, factory, and existing tests.<br>• **Deliver:** `src/ragent/schemas/ingest.py` — `IngestMime.PDF = "application/pdf"`; `"pdf"` alias; `IngestMime.PDF` in `BINARY_MIMES`. `src/ragent/pipelines/factory.py` — `"application/pdf"` in `ALLOWED_MIMES`; `IngestMime.PDF` in `_SPLITTER_LABEL`; `self._pdf = _PdfASTSplitter()` in `_MimeAwareSplitter.__init__`; PDF dispatch branch before PPTX. `tests/unit/test_pipeline_routing_v2.py` — negative test updated to `image/png`; PDF happy-path added. `tests/unit/test_ingest_request_schema_v2.py` — PDF enum, inline reject, file accept, alias. `docs/00_spec.md` — §3.1 allow-list, §4.2 table, §4.6 three env vars. | §4.2, §4.6 | [x] | Dev |
| T-PDF.4 | Refactor | • **Achieve:** Address post-review findings: remove redundant batch loop (YAGNI — PyMuPDF holds doc in RAM regardless), add empty-`raw_bytes` guard, make OCR language list env-configurable, log warning on OCR fallback, add `str_env` utility.<br>• **Deliver:** `src/ragent/utility/env.py` — `str_env(var, default)`. `src/ragent/pipelines/factory.py` — drop `PDF_PAGE_BATCH_THRESHOLD/BYTES_THRESHOLD/BATCH_PAGES`; add `PDF_OCR_LANGUAGES = str_env(...)`; `_logger.warning` on OCR fallback; walrus guard + flatten page loop. `tests/unit/test_pdf_ast_splitter.py` — remove `test_pdf_batch_mode_processes_all_pages`; add `test_pdf_empty_bytes_skipped`. `.env.example` + `docs/00_spec.md §4.6` — replace 3 batch vars with `PDF_OCR_LANGUAGES`. | §4.6 | [x] | Dev |
| T-PDF.5 | Green | • **Achieve:** Implement PyMuPDF best-practice OOM prevention: explicit page-proxy release + MuPDF LRU cache eviction after every page bounds peak RSS to one page's intermediate data.<br>• **Deliver:** `src/ragent/pipelines/factory.py` — `del page` before `fitz.TOOLS.store_shrink(100)` in the page loop; updated docstring. `tests/unit/test_pdf_ast_splitter.py` — `test_pdf_store_shrink_called_once_per_page` verifies eviction fires once per page. | §4.2 | [x] | Dev |

### Manual rerun endpoint (branch `claude/add-rerun-ingest-endpoint-SEriU`) — 2026-05-14

> Source: user request 2026-05-14. Operators need a manual way to (re)dispatch the ingest pipeline for a document — the FAILED tail (post `attempt > 5` reconciler cap) had no escape, and stuck UPLOADED/PENDING had to wait 5 min for the reconciler. Semantically a "manual run" rather than strictly a "rerun" (accepts any non-READY/non-DELETING state).

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-RERUN.1 | Red+Green | • **Achieve:** Add `DocumentRepository.mark_for_rerun(document_id) -> Literal['ok','not_rerunnable','not_found']` — atomic conditional UPDATE transitioning `UPLOADED\|PENDING\|FAILED → PENDING` while clearing `error_code`/`error_reason`, refreshing `updated_at`, and resetting `attempt` to 0 (so the reconciler's `_mark_failed` budget check doesn't immediately re-FAIL an exhausted rerun). Distinguishes 404 vs 409 via a follow-up `SELECT 1` only when the UPDATE matched no row.<br>• **Deliver:** `src/ragent/repositories/document_repository.py::mark_for_rerun`; `tests/unit/test_document_repository.py` — 7 tests covering each pre-state (UPLOADED/PENDING/FAILED → "ok"; READY/DELETING → "not_rerunnable"; missing → "not_found"; attempt-reset regression). | §3.1 / S41 | [x] | Dev |
| T-RERUN.2 | Red+Green | • **Achieve:** Add `IngestService.rerun(document_id)` and `DocumentNotRerunnable` exception. Service calls `repo.mark_for_rerun`, dispatches `not_found → DocumentNotFound`, `not_rerunnable → DocumentNotRerunnable`, `ok → broker.enqueue("ingest.pipeline", document_id=...)`.<br>• **Deliver:** `src/ragent/services/ingest_service.py::rerun` + `DocumentNotRerunnable`; `tests/unit/test_ingest_service_rerun.py` — 3 tests covering happy path + both error branches; broker not called on failure. | §3.1 / S41 | [x] | Dev |
| T-RERUN.3 | Red+Green | • **Achieve:** Add `POST /ingest/v1/{document_id}/rerun` returning 202 / 404 `INGEST_NOT_FOUND` / 409 `INGEST_NOT_RERUNNABLE`. New `HttpErrorCode.INGEST_NOT_RERUNNABLE`. Spec endpoint table row, BDD scenario S41, and error catalog row added.<br>• **Deliver:** `src/ragent/routers/ingest.py` (new endpoint); `src/ragent/errors/codes.py` (new enum member); `tests/unit/test_ingest_router_v2.py` — 3 tests (202 happy / 404 / 409); `docs/00_spec.md` — endpoint table, S41, error catalog. | §4.1 / §4.1.2 / S41 | [x] | Dev |

### HTTP upstream error logging (branch `claude/add-http-logging-NZ1tu`) — 2026-05-14

> Source: user request 2026-05-14. Centralise diagnostic logging on the two shared `httpx.Client` objects so every upstream failure (J1 auth, embedding, LLM, rerank, unprotect) emits the request body, response body (when available), redacted headers, status, and exception type. Bodies are emitted under non-denylisted keys (`http_request_payload` / `http_response_payload`) — a narrow carve-out from the Logging Rule documented in `docs/00_journal.md` (Spec). Auth headers (`Authorization`, `apikey`, `Cookie`) and the J1 `key` field of the auth POST are always redacted; bodies are truncated at `HTTP_ERROR_LOG_MAX_BYTES` (default 8 KB) with `request_truncated` / `response_truncated` flags.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-HTTPLOG.1 | Red | • **Achieve:** Pin `install_error_logging(client, *, client_name, redact_auth_body=False)` contract: success path emits no log; non-2xx responses and `httpx.HTTPError` subclasses (timeout, connect, read) emit a single `http.upstream_error` structured record carrying method/url/status/exception_type/headers/`http_request_payload`/`http_response_payload`/`request_truncated`/`response_truncated`; redaction of `Authorization`/`apikey`/`Cookie` headers and (when `redact_auth_body=True`) the JSON `key` field; truncation at `HTTP_ERROR_LOG_MAX_BYTES`; streaming responses (`client.stream()`) skip response-body capture; original exception re-raised unchanged.<br>• **Deliver:** `tests/unit/test_http_error_logging.py` covering: `test_success_response_emits_no_log`, `test_4xx_response_logs_request_and_response_body`, `test_5xx_response_logs_request_and_response_body`, `test_timeout_exception_logs_request_body_only`, `test_connect_error_logs_request_body_only`, `test_streaming_response_error_logs_without_response_body`, `test_auth_body_key_field_is_redacted`, `test_authorization_header_is_redacted`, `test_apikey_and_cookie_headers_are_redacted`, `test_body_over_8kb_is_truncated_with_flag`, `test_non_utf8_body_decoded_with_replacement`, `test_max_bytes_env_var_overrides_default`, `test_exception_is_reraised_after_logging`, `test_install_marks_client_with_attribute`. | §4.6.8 | [x] | QA |
| T-HTTPLOG.2 | Green | • **Achieve:** Implement the hook factory and wire it into both shared httpx clients in the composition root.<br>• **Deliver:** `src/ragent/bootstrap/http_logging.py` — `install_error_logging()` wraps `client.send`, captures request body before send, branches on response status vs exception, decodes bytes with `errors="replace"`, truncates by `HTTP_ERROR_LOG_MAX_BYTES`, redacts auth headers and (optionally) the JSON `key` field, emits a single `logger.error("http.upstream_error", ...)`, re-raises captured exceptions. Sets `client.__ragent_http_error_logging__ = True` marker. `src/ragent/bootstrap/composition.py:57-58` — call `install_error_logging(http, client_name="upstream")` and `install_error_logging(auth_http, client_name="auth", redact_auth_body=True)`. `.env.example` + `docs/00_spec.md` §4.6.8 — new `HTTP_ERROR_LOG_MAX_BYTES` row (default 8192). | §4.6.8 | [x] | Dev |
| T-HTTPLOG.3 | Refactor | • **Achieve:** /simplify + /review pass; journal-add row in `docs/00_journal.md` (Spec) recording the deliberate `http_request_payload` / `http_response_payload` denylist carve-out for upstream-error diagnostics. | §4.6.8 | [ ] | Dev |
### Security file-upload checks (branch `claude/security-file-upload-checks-ac4Cv`) — 2026-05-14

> Source: user request 2026-05-14. Binary ingest MIME types (DOCX/PPTX/PDF) are
> currently parsed with python-docx / python-pptx / fitz with no guard against
> archive-decompression bombs. A 50 MB upload can expand to many GB inside the
> TaskIQ worker. Add a layered, low-cost preflight: magic-byte check at the
> upload route, zip preflight (members / ratio / expanded / per-member /
> traversal) before docx/pptx parse, PDF page-count cap before per-page
> extraction. All thresholds env-overridable; rejections emit a typed domain
> exception with `error_code=INGEST_ARCHIVE_UNSAFE` (or `INGEST_MAGIC_MISMATCH` /
> `INGEST_PDF_TOO_MANY_PAGES`) and increment a Prometheus counter.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-SEC.1 | Red | • **Achieve:** Pin magic-byte rejection at `POST /ingest/v1/upload` for DOCX/PPTX/PDF MIME with mismatched file signature.<br>• **Deliver:** `tests/unit/test_ingest_upload_magic_byte.py` — uploads declaring `mime_type=docx/pptx` with first bytes ≠ `PK\x03\x04` → 415 `INGEST_MAGIC_MISMATCH`; `mime_type=pdf` with first bytes ≠ `%PDF-` → 415; valid signatures pass through to service layer. Text MIMEs bypass the check. | §4.2 | [x] | QA |
| T-SEC.2 | Green | • **Achieve:** Implement magic-byte validator at upload route + shared signature table.<br>• **Deliver:** `src/ragent/security/file_signature.py` — `MIME_SIGNATURES: dict[IngestMime, bytes]`, `assert_magic_byte(mime, raw) -> None` raising `MagicByteMismatchError(error_code='INGEST_MAGIC_MISMATCH', http_status=415)`. `src/ragent/routers/admin_ingest.py::upload` calls it after `file.read()`. | §4.2 | [x] | Dev |
| T-SEC.3 | Red | • **Achieve:** Pin zip-archive preflight contract for DOCX/PPTX: rejects on member-count, ratio, total-expanded, per-member, and path-traversal violations.<br>• **Deliver:** `tests/unit/test_archive_guard.py` — bombs with `len(infolist()) > INGEST_MAX_ARCHIVE_MEMBERS` rejected; `Σ file_size / len(raw) > INGEST_MAX_ARCHIVE_RATIO` rejected; `Σ file_size > INGEST_MAX_ARCHIVE_EXPANDED_BYTES` rejected; single member > expanded cap rejected; member name with `..` or leading `/` rejected; normal docx/pptx fixtures pass. All raise `ArchiveBombError(error_code='INGEST_ARCHIVE_UNSAFE', http_status=413)`. | §4.2 | [x] | QA |
| T-SEC.4 | Green | • **Achieve:** Implement `assert_safe_zip` and wire it into DOCX/PPTX splitters before `DocxDocument` / `Presentation` construction.<br>• **Deliver:** `src/ragent/security/archive_guard.py` — `assert_safe_zip(raw, *, max_members, max_ratio, max_expanded) -> None` reading only zip central directory (no inflate); env constants `INGEST_MAX_ARCHIVE_MEMBERS=5000`, `INGEST_MAX_ARCHIVE_RATIO=100`, `INGEST_MAX_ARCHIVE_EXPANDED_BYTES=524288000`. `src/ragent/pipelines/factory.py` — call at line 322 (DOCX) and line 364 (PPTX) before opening the file. | §4.2, §4.6 | [x] | Dev |
| T-SEC.5 | Red | • **Achieve:** Pin PDF page-count cap before per-page extraction.<br>• **Deliver:** `tests/unit/test_pdf_ast_splitter.py::test_pdf_page_count_exceeds_cap_raises` — PDF with `page_count > INGEST_MAX_PDF_PAGES` (test fixture monkeypatches to 2) raises `PdfTooManyPagesError(error_code='INGEST_PDF_TOO_MANY_PAGES', http_status=413)`; PDFs ≤ cap unaffected. | §4.2 | [x] | QA |
| T-SEC.6 | Green | • **Achieve:** Implement page-count guard in `_PdfASTSplitter` + env var.<br>• **Deliver:** `src/ragent/security/archive_guard.py::assert_safe_pdf_page_count(pdf)` raising `PdfTooManyPagesError`; `INGEST_MAX_PDF_PAGES=2000` env constant; `src/ragent/pipelines/factory.py::_PdfASTSplitter.run` checks `pdf.page_count` immediately after `fitz.open(...)`. | §4.2, §4.6 | [x] | Dev |
| T-SEC.7 | Behavioral | • **Achieve:** Expose Prometheus counter for guard rejections so over-tight thresholds surface in Grafana.<br>• **Deliver:** `src/ragent/bootstrap/metrics.py` — `ragent_ingest_rejected_total{reason}` counter + `record_ingest_rejection(reason)` helper with `reason ∈ {magic, invalid, members, ratio, expanded, per_member, traversal, pdf_pages}` (closed set; unknown raises). Emission sites: `MagicByteMismatchError`/`ArchiveBombError`/`PdfTooManyPagesError.__init__`. `tests/unit/test_metrics_ingest_rejected.py` parametrizes one assertion per reason plus end-to-end emission tests from each guard. | §4.6 | [x] | Dev |
| T-SEC.8 | Refactor | • **Achieve:** Update spec + env-var inventory (closed-system rule per `docs/00_journal.md` 2026-05-04 Config).<br>• **Deliver:** `docs/00_spec.md §4.1.2` (error catalogue) gains rows for `INGEST_MAGIC_MISMATCH` / `INGEST_ARCHIVE_UNSAFE` / `INGEST_PDF_TOO_MANY_PAGES`; `docs/00_spec.md §3.1` validation-order prose covers magic-byte + archive preflight + page-count cap; `docs/00_spec.md §4.6` env-var table already populated by T-SEC.3/.4 and T-SEC.5/.6 drift fixes; `.env.example` already mirrored; `docs/API.md` GET-status section documents the terminal `error_code` values. | §4.2, §4.6 | [x] | Dev |

### Embedding-model lifecycle (branch `claude/design-embedding-model-switch-5N3Uh`) — 2026-05-15

> Source: B50. Multi-vector single-index swap with five admin APIs and a CANDIDATE/CUTOVER state machine. Design doc: `docs/team/2026_05_15_embedding_model_lifecycle.md`.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-EM.0 | Analysis | • **Achieve:** Lock the multi-vector single-index swap design and APIs.<br>• **Deliver:** `docs/team/2026_05_15_embedding_model_lifecycle.md` + B50 row in `docs/00_spec.md` §7. | B50 | [x] | Architect |
| T-EM.1 | Red | • **Achieve:** Pin the embedding-lifecycle state machine: allowed transitions `{IDLE→promote→CANDIDATE, CANDIDATE→cutover→CUTOVER, CANDIDATE→abort→IDLE, CUTOVER→rollback→CANDIDATE, CUTOVER→commit→IDLE}`; rejected transitions raise `IllegalEmbeddingTransition`.<br>• **Deliver:** `tests/unit/test_embedding_lifecycle_state_machine.py` — happy paths + rejected paths + idempotent re-promote rejection. | B50 | [x] | QA |
| T-EM.2 | Green | • **Achieve:** Implement the embedding-lifecycle state machine.<br>• **Deliver:** `src/ragent/utility/embedding_lifecycle.py` — `next_state(state, action)` pure function over a `frozen dict` transition table; raises `IllegalEmbeddingTransition` on any unknown state, unknown action, or disallowed pair. Business logic ≤ 30 LOC (achieved: 13 LOC). | B50 | [x] | Dev |
| T-EM.3 | Red | • **Achieve:** Pin `EmbeddingModelConfig` dataclass: name + dim + api_url + model_arg → derived `field` name; normalization (`bge-m3` → `bgem3`); dim range `[1, 4096]` enforced.<br>• **Deliver:** `tests/unit/test_embedding_model_config.py`. | B50 | [x] | QA |
| T-EM.4 | Green | • **Achieve:** Implement `EmbeddingModelConfig`.<br>• **Deliver:** `src/ragent/clients/embedding_model_config.py`. | B50 | [x] | Dev |
| T-EM.5 | Structural | • **Achieve:** Persist lifecycle settings in MariaDB.<br>• **Deliver:** `migrations/009_system_settings.sql` + `migrations/schema.sql` update — `system_settings(id PK, setting_key UNIQUE, setting_value JSON, updated_at)`; seed four rows for `embedding.stable`/`candidate`/`read`/`retired`. | B50 | [x] | Dev |
| T-EM.6 | Red | • **Achieve:** Pin `SystemSettingsRepository` contract: typed reads for the four embedding rows, atomic transition writes, optimistic-locked transitions.<br>• **Deliver:** `tests/unit/test_system_settings_repository.py`. | B50 | [x] | QA |
| T-EM.7 | Green | • **Achieve:** Implement repository.<br>• **Deliver:** `src/ragent/repositories/system_settings_repository.py`. | B50 | [x] | Dev |
| T-EM.8 | Red | • **Achieve:** Pin `ActiveModelRegistry` cache contract: `read_model()`, `write_models()`, TTL refresh, stale-on-failure.<br>• **Deliver:** `tests/unit/test_active_model_registry.py`. | B50 | [x] | QA |
| T-EM.9 | Green | • **Achieve:** Implement `ActiveModelRegistry`.<br>• **Deliver:** `src/ragent/services/active_model_registry.py`. | B50 | [x] | Dev |
| T-EM.10 | Red | • **Achieve:** Pin cutover preflight: hard gates (`state_is_candidate`, `field_dim_matches`, `candidate_coverage`, `dual_write_warmup`); soft gates with `force` bypass.<br>• **Deliver:** `tests/unit/test_cutover_preflight.py`. | B50 | [x] | QA |
| T-EM.11 | Green | • **Achieve:** Implement preflight.<br>• **Deliver:** `src/ragent/services/embedding_lifecycle_service.py::cutover_preflight` (and the four other lifecycle methods). | B50 | [x] | Dev |
| T-EM.12 | Red | • **Achieve:** Pin admin router for the five lifecycle endpoints + state + preflight; RFC 9457 errors on state-machine rejections (409 `EMBEDDING_LIFECYCLE_INVALID_STATE`).<br>• **Deliver:** `tests/unit/test_admin_embedding_router.py`. | B50 | [x] | QA |
| T-EM.13 | Green | • **Achieve:** Implement admin router.<br>• **Deliver:** `src/ragent/routers/admin_embedding.py` defining `create_router(service, snapshot_provider)` at prefix `/embedding/v1`. Mounting in `bootstrap/app.py` is deferred to T-EM.21 alongside `ActiveModelRegistry` wiring (the snapshot_provider depends on the composition-root registry singleton, which doesn't exist until T-EM.21). | B50 | [x] | Dev |
| T-EM.14 | Red | • **Achieve:** Pin ingest dual-write: `DocumentEmbedder` reads `registry.write_models()`, embeds with each, populates `embedding_<m>_<d>` fields on chunks.<br>• **Deliver:** `tests/unit/test_document_embedder_dual_write.py`. | B50 | [x] | QA |
| T-EM.15 | Green | • **Achieve:** Implement dual-write embedder.<br>• **Deliver:** Update `src/ragent/pipelines/factory.py::DocumentEmbedder` and `resources/es/chunks_v1.json` to allow dynamic vector field names. | B50 | [x] | Dev |
| T-EM.16 | Red | • **Achieve:** Pin query path uses `registry.read_model()` to pick the field name for kNN.<br>• **Deliver:** `tests/unit/test_query_embedder_dynamic_field.py`. | B50 | [x] | QA |
| T-EM.17 | Green | • **Achieve:** Implement dynamic field selection in `_QueryEmbedder` and downstream retriever wiring.<br>• **Deliver:** Update `src/ragent/pipelines/chat.py`. | B50 | [x] | Dev |
| T-EM.18 | Red | • **Achieve:** Pin retired-field reconciler arm: poll `embedding.retired`, batched `_update_by_query` removes values, patches `cleanup_done`.<br>• **Deliver:** `tests/unit/test_retired_embedding_sweep.py`. | B50 | [x] | QA |
| T-EM.19 | Green | • **Achieve:** Implement reconciler arm.<br>• **Deliver:** Update `src/ragent/reconciler.py` with `retired_embedding_sweep`. | B50 | [x] | Dev |
| T-EM.20 | Red | • **Achieve:** End-to-end lifecycle: promote → backfill → cutover → rollback → abort over real MariaDB + ES; integration test asserts ES mapping, dual-write coverage, settings transitions, and rollback-window write safety.<br>• **Deliver:** `tests/integration/test_embedding_lifecycle_e2e.py` (testcontainers `@pytest.mark.docker`). | B50 | [x] | QA |
| T-EM.21 | Green | • **Achieve:** Wire `ActiveModelRegistry` into composition root, replace hardcoded `bge-m3` / 1024.<br>• **Deliver:** `src/ragent/bootstrap/composition.py` + `src/ragent/clients/embedding.py` (constructor `model: str`) + `.env.example` (`EMBEDDING_REGISTRY_TTL_SECONDS=10`, `COMMIT_MIN_HOURS=24`). | B50 | [x] | Dev |

### Feedback retrieval signal (branch `claude/investigate-rrf-retrieval-pDFt4`) — 2026-05-16

> **Goal (B54-B57; originally drafted as B50-B53, renumbered after collision with main's embedding-lifecycle B50):** Introduce a 3rd retriever (`_FeedbackMemoryRetriever`) that boosts sources liked on semantically-similar past queries. Closed loop: `/chat` emits `request_id + feedback_token` (HMAC, 7-day TTL, B55) → `POST /feedback/v1` dual-writes MariaDB `feedback` (meta) + ES `feedback_v1` (query_text + query_embedding) → next chat consults `feedback_v1` via kNN + Wilson lower bound + 14-day half-life decay → weighted RRF (3-way, weight 0.5). Default `CHAT_FEEDBACK_ENABLED=false`; ship dark. Reason enum frozen at 6 values (B56). IPS / exploration / fine-tune deferred to P2+ (B57).

| Task | Type | Deliverables | Dep | Status | Owner |
|---|---|---|---|---|---|
| T-FB.1 | Red+Green | • **Achieve:** Pin `feedback_token` contract — `sign(payload, secret) → token` / `verify(token, secret) → payload` with TTL enforcement and tamper detection (B55).<br>• **Deliver:** `src/ragent/utility/feedback_token.py` — payload shape `{request_id, user_id, sources_hash, ts}`; TTL = 7 days; `TokenExpired` / `TokenTampered` / `TokenInvalid` exceptions. `tests/unit/test_feedback_token.py` — roundtrip happy path; single-byte tamper detected; `ts` more than 7 days old → `TokenExpired`; wrong secret → `TokenTampered`; malformed token → `TokenInvalid`. | B55 | [x] | Dev |
| T-FB.2 | Red+Green | • **Achieve:** Pin `wilson_lower_bound(positives, total, z)` math (B54).<br>• **Deliver:** `src/ragent/utility/wilson.py` — single function, `z=1.96` default. `tests/unit/test_wilson.py` — `total=0` returns 0; monotonicity in positives; known reference values (e.g. `wilson_lb(3, 3) < wilson_lb(100, 100)`); always in [0, 1]. | B54 | [x] | Dev |
| T-FB.3 | Structural | • **Achieve:** Create `feedback` table per B54/B55 honouring §5.3 UID rule and a `FeedbackRepository` skeleton (no behaviour yet — Red phase of T-FB.4 will drive `upsert` shape).<br>• **Deliver:** `migrations/010_feedback.sql` — `CREATE TABLE feedback (feedback_id CHAR(26) PRIMARY KEY, request_id CHAR(26) NOT NULL, user_id VARCHAR(64) NOT NULL, source_app VARCHAR(64) NOT NULL, source_id VARCHAR(128) NOT NULL, vote TINYINT NOT NULL, reason VARCHAR(32) NULL, position_shown SMALLINT NULL, created_at DATETIME(6) NOT NULL, updated_at DATETIME(6) NOT NULL, UNIQUE KEY uq_user_req_app_src (user_id, request_id, source_app, source_id))`. **Renumbered from 009 after collision with main's `009_system_settings.sql`.** `src/ragent/repositories/feedback_repository.py` — class skeleton; `feedback_id` generated by `new_id()` (§5.3). `docs/00_spec.md §5.1` — append `feedback` table block. | B54 | [x] | Dev |
| T-FB.4 | Red+Green | • **Achieve:** Implement `FeedbackRepository.upsert(...)` — idempotent on `(user_id, request_id, source_app, source_id)`; second call with different `vote` overwrites prior (last-write-wins, observable via `updated_at` advance).<br>• **Deliver:** `tests/unit/test_feedback_repository.py` — insert returns new row; second insert same key updates `vote`/`reason`/`updated_at`; NULL `reason` accepted; `vote ∈ {+1, -1}` enforced by DB CHECK or service layer. `FeedbackRepository.upsert` implementation using SQL `INSERT ... ON DUPLICATE KEY UPDATE`. | T-FB.3 | [x] | QA / Dev |
| T-FB.5 | Structural | • **Achieve:** Add ES `feedback_v1` index resource + bootstrap PUT, matching `chunks_v1` pattern (B54).<br>• **Deliver:** `resources/es/feedback_v1.json` — mapping with `query_text:text/icu_text`, `query_embedding:dense_vector(1024,cosine,flat)`, `source_id:keyword`, `vote:byte`, `reason:keyword`, `source_app:keyword`, `source_meta:keyword`, `user_id_hash:keyword`, `ts:date`. `src/ragent/bootstrap/init_schema.py` — extend to `PUT /feedback_v1` if absent. `tests/resources/es/feedback_v1.json` — test-mode variant with `standard` analyzer (B42 pattern). `tests/integration/test_es_resource_drift.py` — extend drift check to `feedback_v1`. `docs/00_spec.md §5.4` — new subsection mirroring §5.2 structure. | B54 | [x] | Dev |
| T-FB.6 | Red+Green | • **Achieve:** Implement `POST /feedback/v1` end-to-end — HMAC verify → snapshot validation → re-embed → dual-write (MariaDB-first, ES-second per B55).<br>• **Deliver:** `src/ragent/schemas/feedback.py` — `FeedbackRequest` Pydantic model with reason enum (B56 6 values) + `(source_app, source_id)` pair + `shown_sources: list[SourceRef]`. `src/ragent/routers/feedback.py` — verifies HMAC, asserts body `request_id == payload.request_id` and (when `X-User-Id` present) `X-User-Id == payload.user_id`, checks voted pair ∈ shown_sources, calls `EmbeddingClient.embed([query_text], query=True)`, calls `FeedbackRepository.upsert` (MariaDB SoT) then ES index write with `source_app` field + `es_id = sha256(user|request|source_app|source_id)`. ES failure logs `event=feedback.es_write_failed` + increments `ragent_feedback_es_write_failed_total` but still returns 204. Errors: `401 FEEDBACK_TOKEN_INVALID` / `410 FEEDBACK_TOKEN_EXPIRED` / `422 FEEDBACK_SOURCE_INVALID` / `422 FEEDBACK_VALIDATION` (RFC 9457). `tests/unit/test_feedback_router.py` covers all branches incl. PR #80 codex P1 (request_id replay / cross-user reuse). `docs/00_spec.md §3.4.5` (new) + §4.1 endpoint row + §4.1.2 error codes. | T-FB.1, T-FB.4, T-FB.5 | [x] | QA / Dev |
| T-FB.7 | Red+Green | • **Achieve:** Implement `_FeedbackMemoryRetriever` Haystack component (B54). Adds `DocumentRepository.get_document_ids_by_source(pairs)` to translate `feedback_v1`'s (source_app, source_id) key into the current READY document_id needed for `chunks_v1` lookup.<br>• **Deliver:** `src/ragent/pipelines/chat.py::_FeedbackMemoryRetriever` — ES kNN on `feedback_v1.query_embedding`, filters `cosine ≥ 0.7` AND `ts > now-90d` AND optional `source_app`/`source_meta`; group by `(source_app, source_id)`; gate `(likes+dislikes) ≥ CHAT_FEEDBACK_MIN_VOTES`; Wilson × decay scoring. MariaDB pair→document_id lookup, then **single ES `terms` query on `chunks_v1.document_id`** (NOT N+1). `tests/unit/test_feedback_retriever.py` (8 tests + filter-bridge unit). | T-FB.2, T-FB.5 | [x] | QA / Dev |
| T-FB.8 | Structural | • **Achieve:** Wire feedback components into composition root + add env vars (B54, B55, B57). Env names use `CHAT_FEEDBACK_*` prefix to match the existing `CHAT_JOIN_MODE` / `CHAT_RERANK_ENABLED` / `CHAT_RATE_LIMIT_*` family in §4.6.6; `FEEDBACK_HMAC_SECRET` keeps the bare prefix (cross-cutting secret).<br>• **Deliver:** `src/ragent/bootstrap/composition.py` — `Container.feedback_repository`, `Container.feedback_hmac_secret`; reads `CHAT_FEEDBACK_ENABLED` (default `false`), `CHAT_FEEDBACK_RRF_WEIGHT` (0.5), `CHAT_FEEDBACK_MIN_VOTES` (3), `CHAT_FEEDBACK_HALF_LIFE_DAYS` (14), `FEEDBACK_HMAC_SECRET` (required when feedback enabled). `src/ragent/bootstrap/app.py` — conditional `create_feedback_router(...)` when `feedback_hmac_secret is not None`. `_FeedbackMemoryRetriever` adds structured-logging events using counts + bools only. `docs/00_spec.md §4.6.6` — five new env var rows. `.env.example` — same five lines. | T-FB.4, T-FB.7 | [x] | Dev |
| T-FB.9 | Red+Green | • **Achieve:** Pipeline factory adds 3rd retriever when feedback enabled, RRF weights become `[1.0, 1.0, CHAT_FEEDBACK_RRF_WEIGHT]` (B54). Haystack normalises weights to sum=1, preserving the ratio. Feedback retriever MUST be connected LAST so the third weight slot lands on it (PR #80 gemini high).<br>• **Deliver:** `src/ragent/pipelines/chat.py::build_retrieval_pipeline` — accepts optional `feedback_retriever` + `feedback_weight`. `src/ragent/bootstrap/composition.py` — constructs `_FeedbackMemoryRetriever`. `tests/unit/test_pipeline_factory_join_mode.py` — matrix cases + connection-order assertion. `docs/00_spec.md §3.4` diagram updated. | T-FB.7, T-FB.8 | [x] | QA / Dev |
| T-FB.10 | Red+Green | • **Achieve:** `/chat/v1` (and stream `done` event) response carries `request_id` + `feedback_token` when feedback enabled AND `X-User-Id` is present (B55).<br>• **Deliver:** `src/ragent/routers/chat.py` — `create_chat_router(..., feedback_hmac_secret)`; `_maybe_mint_feedback_envelope` computes `sources_hash` over `(source_app, source_id)` **pair** list (PR #80 gemini security-high). `tests/unit/test_chat_router_feedback_token.py` — pair binding asserted. `docs/00_spec.md §3.4.2` + `docs/API.md` updated. | T-FB.1 | [x] | QA / Dev |
| T-FB.11 | Red+Green | • **Achieve:** End-to-end feedback loop integration: real chat + feedback routers wired in-process with mocked ES / MariaDB / embedder.<br>• **Deliver:** `tests/integration/test_feedback_loop.py` — round-trip, 3 distinct users, cross-user token relay rejected (PR #80 codex P1), shown_sources tamper rejected. | T-FB.6, T-FB.9, T-FB.10 | [x] | QA |
| T-FB.12 | Refactor | • **Achieve:** Capture the architectural lesson + log-disclosure invariant + PR #80 review findings in the project journal.<br>• **Deliver:** `docs/00_journal.md` rows covering: HMAC-bound snapshot vs eager DB write; text-in-ES / meta-in-MariaDB invariant; 3rd Haystack retriever vs ES-DSL fusion; one-MariaDB + one-ES-terms-query pattern; source-level vs chunk-level rationale; log-disclosure invariant; pair identity (source_app, source_id); token-bound request_id/user_id (codex P1). | T-FB.11 | [x] | Dev |
### MCP Hub microservice (branch `claude/mcp-hub-dynamic-apis-gZ4Jn`) — 2026-05-16

> Source: user kickoff. Standalone FastMCP service that loads `tools.yaml` at
> startup and dynamically registers each REST endpoint as an MCP Tool, with
> `inspect.Signature`/`inspect.Parameter` driving FastMCP's Pydantic/JSON
> Schema inference. Streamable HTTP transport. Used by remote agents
> (e.g. Haystack) to call upstream REST APIs without hardcoded clients.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-MH.0 | Kickoff | • **Achieve:** Land the dynamic Hub skeleton — YAML schema, signature factory, httpx forwarder, lifespan-managed client, Streamable HTTP entry point.<br>• **Deliver:** `src/ragent/mcp_hub/{mcp_hub.py,server.py,tools.example.yaml,__init__.py}` + `tests/unit/mcp_hub/test_signature_factory.py` (5 tests covering parse validation, required/optional signature shape, end-to-end multi-location request dispatch via `httpx.MockTransport`, YAML round-trip). | — | [x] | Dev |
| T-MH.1 | Spec | • **Achieve:** Document the Hub microservice in `docs/00_spec.md` — tools.yaml schema, env-var inventory (`MCP_HUB_*`), Streamable HTTP endpoint contract, deployment topology.<br>• **Deliver:** new `docs/00_spec.md §3.9 MCP Hub Microservice` section + README quickstart block. | — | [x] | Spec |
| T-MH.2 | Test | • **Achieve:** Add an integration test that boots the Hub against a stub upstream and exercises one tool call over Streamable HTTP via a FastMCP client. Also fix `server.py` lifespan composition for `fastmcp>=3.0` (`http_app()` no longer accepts `lifespan=`); extract `build_app(bundle)` so test and `main()` share the same boot path.<br>• **Deliver:** `tests/integration/test_mcp_hub_streamable_http.py` (2 tests: list + invoke against `pytest-httpserver` stub) + refactored `src/ragent/mcp_hub/server.py`. | — | [x] | QA |
| T-MH.3 | Hardening | • **Achieve:** Pre-compute per-tool wire dicts (header-name kebab map, partitioned param lists) and connection limits from `defaults`; consider auth header pass-through.<br>• **Deliver:** updates to `src/ragent/mcp_hub/mcp_hub.py`. | — | [ ] | Dev |
| T-MH.4 | Behavioral | • **Achieve:** Upstream-error transparency contract — replace blanket `raise_for_status` with structured envelopes: 2xx→`{ok:True,...}`, 4xx→`{ok:False, error: {upstream_body, truncated, upstream_request_id}}` (≤4KB, JSON+text passed through, HTML dropped), 5xx→`ToolError` carrying status + request_id ONLY (body redacted to prevent stack-trace / SQL leakage), `httpx.TimeoutException`→`ToolError(type=timeout)`, `httpx.ConnectError`→`ToolError(type=connect_error)`. Hub configuration errors continue to bubble as JSON-RPC errors.<br>• **Deliver:** `src/ragent/mcp_hub/mcp_hub.py` (`_build_4xx_error`, `_extract_request_id`, refactored `_call`) + `tests/unit/mcp_hub/test_error_handling.py` (9 tests). | — | [x] | Dev |
| T-MH.5 | Behavioral | • **Achieve:** Static `tools.yaml` validator runnable in CI — catches duplicate names, path placeholders without matching `location: path` parameter, path parameters not referenced in template, body parameters on non-body-accepting HTTP methods, plus the existing `load_tools_yaml` schema errors.<br>• **Deliver:** `src/ragent/mcp_hub/doctor.py` (CLI exit 0/1/2) + `Makefile` target `mcp-hub-doctor` chained into `check` + `tests/unit/mcp_hub/test_doctor.py` (9 tests). | — | [x] | Dev |
| T-MH.6 | Behavioral | • **Achieve:** Address gemini-code-assist PR #79 review (three medium-priority findings): (a) 2xx with `Content-Type: application/json` but malformed body crashed `_call` — now falls back to `resp.text`; (b) doctor surfaces missing `defaults.base_url` only when at least one tool path is relative (absolute-URL paths still accepted); (c) `MCP_HUB_PORT=<non-numeric>` now exits with a friendly `SystemExit` message instead of an `int()` stack trace.<br>• **Deliver:** patches to `src/ragent/mcp_hub/{mcp_hub.py,doctor.py,server.py}` + 4 new tests (2xx malformed JSON fallback, base_url missing with relative path flagged, base_url missing with absolute path ok, non-numeric port SystemExit). | — | [x] | Dev |
| T-MH.7 | Behavioral | • **Achieve:** Heterogeneous-upstream support — per-tool `base_url` override (multiple domains), per-tool `static_headers` with `${ENV_VAR}` substitution resolved at startup (covers Bearer / API-key / any header-based auth via a single mechanism), per-tool `forward_headers` (incoming→outgoing name map) propagating MCP-client headers like `X-User-Id` to upstreams via a request-scoped `ContextVar` populated by `HeaderForwardMiddleware`. Same-header overlap between static and forward is rejected at load time. Doctor relaxes the missing-base_url rule to accept per-tool overrides.<br>• **Deliver:** `src/ragent/mcp_hub/mcp_hub.py` (`_INCOMING_HEADERS`, `_ENV_REF`, `_resolve_env_refs`, `_parse_headers`, extended `_ToolSpec` + `_parse_tool`, updated `_make_tool_callable` URL builder + header merge) + `src/ragent/mcp_hub/server.py` (`HeaderForwardMiddleware` wrapping the FastMCP ASGI app) + `src/ragent/mcp_hub/doctor.py` (per-tool override path) + `tests/unit/mcp_hub/test_multi_upstream.py` (8 tests) + `tests/unit/mcp_hub/test_middleware.py` (3 tests) + `tools.example.yaml` showcase. | — | [x] | Dev |
| T-MH.8a | Behavioral | • **Achieve:** Header model rework (breaking change to T-MH.7): drop `${ENV_VAR}` substitution from `static_headers` (Hub no longer reads tokens from env — identity flows per-request via MCP-client headers); flip `forward_headers` schema so the VALUE is a template string with `{x-header-name}` placeholders referencing incoming headers (key = outgoing header name, value = how to render). Missing placeholder → skip that outgoing header (graceful degradation). Enables `Authorization: "Bearer {x-jwt-token}"`-style construction and composition like `"user={x-user-id};tenant={x-tenant-id}"`.<br>• **Deliver:** `src/ragent/mcp_hub/mcp_hub.py` (remove `_resolve_env_refs`/`_ENV_REF`, add `_TEMPLATE_PLACEHOLDER`/`_render_forward_template`, simplify `_parse_headers`/`_parse_tool` collision check, update `_make_tool_callable` to template each forward header) + `src/ragent/mcp_hub/server.py` (HeaderForwardMiddleware docstring documents the security boundary) + `tools.example.yaml` (new template syntax, security warning) + updated `test_multi_upstream.py` (12 tests covering literal static, template substitution, prefix wrapping, multi-placeholder, missing-placeholder skip, no-context skip, collision rules). | — | [x] | Dev |
| T-MH.8b | Behavioral | • **Achieve:** Multi-system directory loading with per-system isolation. `MCP_HUB_TOOLS_YAML` accepts a file or a directory; each `*.yaml`/`*.yml` file in the directory is one SYSTEM with name = filename stem (overridable via top-level `system:`). Tool names auto-qualified as `<system>.<tool>` so different systems can reuse raw names. Each system gets its own `httpx.AsyncClient` (`defaults.timeout`, `defaults.max_connections`, `defaults.headers`) — slow systems can't starve other systems' pools. Per-tool `timeout` overrides the system default via `client.request(..., timeout=...)`. Fault isolation: bad yaml syntax → skip whole file; bad tool schema → skip that tool; `add_tool` exception → skip that registration; all failures collected on `HubBundle.failures` so healthy tools still serve. `load_tools_yaml(path, strict=...)` returns `LoadResult` (tools + systems + failures) — doctor uses strict=False to list ALL errors in one run, runtime uses strict=False to keep the Hub up.<br>• **Deliver:** `src/ragent/mcp_hub/mcp_hub.py` (`_SystemSpec`, `LoadFailure`, `LoadResult`, `HubBundle` dataclasses; extended `_ToolSpec` with `system`, `timeout`; rewritten `load_tools_yaml` + `_load_one_file` + `_parse_system_spec`; rewritten `build_hub`) + `src/ragent/mcp_hub/server.py` (consume `HubBundle`, lifespan closes all per-system clients, startup log) + `src/ragent/mcp_hub/doctor.py` (use non-strict load, report ALL failures, per-tool base_url check) + new `tools.example.d/{identity,billing}.yaml` demo (replaces single `tools.example.yaml`) + `Makefile mcp-hub-doctor` target retargeted + new `test_directory_loading.py` (8), `test_system_isolation.py` (3), `test_fault_isolation.py` (5) + updated `test_signature_factory.py` for new return shape. | — | [x] | Dev |
| T-MH.9 | Behavioral | • **Achieve:** Operator-facing structured logging via `structlog` (matching project convention from `src/ragent/services/*` and `src/ragent/middleware/logging.py`). Replace ad-hoc `print()` calls; emit canonical events on every code path: `mcp_hub.ready` (systems list, tool_count fixed bug, failure_count), `mcp_hub.system_configured` (per system: base_url, timeout, max_connections), `mcp_hub.load_failure` (source, reason; WARNING), `mcp_hub.tool_call.success` (INFO; tool, system, status, latency_ms, request_id), `mcp_hub.upstream_4xx` (WARNING; +upstream_request_id), `mcp_hub.upstream_5xx` (ERROR), `mcp_hub.timeout` / `mcp_hub.connect_error` (ERROR), `mcp_hub.shutdown_error` (ERROR with exc_info). Caller `X-Request-Id` propagates into every per-call log via the existing ContextVar. SECURITY: tests pin that rendered header VALUES (Authorization, JWT, API keys) NEVER appear in log output.<br>• **Deliver:** instrumented `src/ragent/mcp_hub/mcp_hub.py` and `src/ragent/mcp_hub/server.py` + new `tests/unit/mcp_hub/test_logging.py` (8 tests covering each event type, request_id propagation, secret-not-in-logs guarantee, fixed tool-count assertion). | — | [x] | Dev |
| T-MH.10 | Behavioral | • **Achieve:** Expose the project's own `POST /retrieve/v1` as an MCP tool by default — bundled `tools.example.d/ragent.yaml` declares system `ragent` pointing at `http://localhost:8000` with one tool `retrieve` whose body parameters mirror `RetrieveRequest` (query/source_app/source_meta/top_k/min_score/dedupe) and forwards X-User-Id from the MCP-client request through to the upstream handler. Operators clone or replace the file per environment (each env's ConfigMap ships its own base_url). Contract pinned by a unit test that loads the actual bundled yaml and asserts the tool surface matches the FastAPI handler's `RetrieveRequest` schema — drift between the router and the yaml breaks the test, not first LLM call.<br>• **Deliver:** new `src/ragent/mcp_hub/tools.example.d/ragent.yaml` + `tests/unit/mcp_hub/test_directory_loading.py::test_bundled_ragent_yaml_exposes_retrieve_tool` (asserts ragent.retrieve name, POST /retrieve/v1, parameter types + defaults, X-User-Id forwarding). | — | [x] | Dev |
| T-MH.11 | Behavioral | • **Achieve:** Operability triple — (a) per-system `defaults.verify_ssl: bool` (default true) flows into `httpx.AsyncClient(verify=...)`, audited via `mcp_hub.system_configured` log; (b) Hub serves `GET /metrics` (Prometheus exposition) as a `Route` sibling to `MCP_HUB_PATH` (canonical path — no 307 redirect for scrapers); (c) `LoadFailure` carries structured `system` / `phase` / `tool` fields, surfaced on both `mcp_hub.load_failure` log AND a new `mcp_hub_tool_load_failures_total{system,phase}` counter. New `mcp_hub_tool_calls_total{system,tool,outcome}` counter + `mcp_hub_tool_call_duration_seconds{system,outcome}` histogram (tool dropped from histogram labels to bound `_bucket` cardinality) cover every outcome in a closed enum {success, upstream_4xx, upstream_5xx, timeout, connect_error}. Definitions in `bootstrap/metrics.py` for import-time singleton; helpers (`record_mcp_hub_load_failure`, `record_mcp_hub_tool_call`) close-enum-guard against caller typos.<br>• **Deliver:** `src/ragent/bootstrap/metrics.py` (new metrics + helpers) + `src/ragent/mcp_hub/mcp_hub.py` (`_SystemSpec.verify_ssl`, `_parse_system_spec` reads `verify_ssl`, `LoadFailure` structured fields, `_record_failure` propagates them, `_call` emits per-outcome metrics, `build_hub` fan-out log+metric) + `src/ragent/mcp_hub/server.py` (`/metrics` Route via `generate_latest`) + `tests/unit/mcp_hub/test_metrics.py` (14 tests: counter/histogram/phase enums, closed-enum collapse, verify_ssl yaml parse + SSLContext check, build_hub structured log + counter) + `tests/integration/test_mcp_hub_streamable_http.py::test_metrics_endpoint_exposes_hub_counters_after_tool_call` + spec §3.9 update (verify_ssl yaml, new §3.9.8 metrics section) + README quickstart note. | — | [x] | Dev |

### Ingest upload discriminator fix (branch `claude/fix-ingest-upload-inline-XEUBM`) — 2026-05-19

> Source: user observation 2026-05-19. `POST /ingest/v1/upload` persisted rows with `ingest_type='inline'` even though `InlineIngestRequest` rejects the binary MIMEs that the upload endpoint accepts — leaving the DB with rows no inline request could ever produce and conflating two cleanup contracts. The fix introduces a dedicated `upload` discriminator value with its own blob-lifecycle rule (worker NEVER auto-deletes; DELETE API is the sole reclaim path).

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-IUP.1 | Red | • **Achieve:** Pin the new discriminator + cleanup contract on the service layer.<br>• **Deliver:** `tests/unit/test_ingest_service_upload.py::test_create_from_upload_records_upload_ingest_type` (asserts `ingest_type=='upload'`, `minio_site is None`); `tests/unit/test_ingest_service_upload.py::test_create_from_upload_emits_log_with_upload_type` (business log carries `ingest_type='upload'`); `tests/unit/test_ingest_service_delete.py::test_delete_upload_ready_deletes_default_site_object` + `…test_delete_file_ready_skips_storage_delete` (DELETE API contract per ingest_type at READY, reusing the existing `_make_doc`/`_make_service` builders); `tests/integration/test_worker_minio_cleanup.py::test_upload_type_ingest_does_not_delete_minio_object` (worker does NOT auto-delete on READY for upload). | §3.1 | [x] | QA |
| T-IUP.2 | Green | • **Achieve:** Add the `upload` enum value end-to-end and wire the new cleanup gate.<br>• **Deliver:** `migrations/011_ingest_type_upload.sql` widens the column to `ENUM('inline','file','upload')` with `ALGORITHM=INSTANT`; `alembic/versions/011_ingest_type_upload.py` wraps it (revises `009`, since `010_feedback.sql` initially had no Alembic wrapper — the wrapper for `010_feedback.sql` was added later as revision `012` chaining off `011`) so `alembic upgrade head` applies the widening in deployments that follow the Alembic path; `migrations/schema.sql` mirrors the change; `src/ragent/services/ingest_service.py::create_from_upload` writes `ingest_type='upload'` and logs the same; `…IngestService.delete` extends the storage-delete gate to fire when `ingest_type=='upload'` regardless of status (worker no longer cleans up after READY for this branch); `docs/API.md` + `docs/00_spec.md` cleanup matrix updated; `src/ragent/routers/admin_ingest.py` module docstring re-aligned. | §3.1 | [x] | Dev |

### ES chunks index config + housekeeping (branch `claude/es-chunk-index-config-g1kf4`) — 2026-05-19

> **Goal:** Three coupled changes on `chunks_v1`:
> (1) flip `embedding.index_options.type` `flat → bbq_hnsw` per user direction (fresh-install only — no reindex; existing data on `flat` MUST be wiped). Overrides spec §5.2 B26 P1 choice; new Decision Log row required.
> (2) Add `indexed_at: date` field populated by an ES **ingest pipeline** (`chunks_default`) referenced via `index.default_pipeline` — best-practice per `docs/00_journal.md` 2026-05-19 Architecture row. No Python writer touches the field.
> (3) Env audit fix: thread `chunks_index_name` (env `ES_CHUNKS_INDEX`) through composition into all consumers; remove the four hardcoded `"chunks_v1"` defaults (`bootstrap/app.py` readyz probe, `reconciler.Reconciler`, `pipelines/chat._FeedbackMemoryRetriever`, `plugins/vector.VectorExtractor`).
>
> **Tidy First**: T-EI.1 (env audit, Structural) lands first as its own commit. T-EI.2/3/4 (schema + pipeline + bbq_hnsw, Behavioral) follow as a second commit. T-EI.5 (spec + Decision Log) consolidates into the Behavioral commit since drift tests would otherwise fail.

| Task | Type | Deliverables | Dep | Status | Owner |
|---|---|---|---|---|---|
| T-EI.1 | Structural | • **Achieve:** Thread `ES_CHUNKS_INDEX` (via `container.chunks_index_name`) through every production caller; remove the four sites that silently fell back to a literal `"chunks_v1"` when the env was overridden.<br>• **Deliver:** `src/ragent/bootstrap/composition.py` — consolidate the duplicated `os.environ.get("ES_CHUNKS_INDEX", ...)` read; pass `chunks_index_name` to `VectorExtractor(index=...)` and `_FeedbackMemoryRetriever(chunks_index=...)`. `src/ragent/reconciler.py::_PerTickRunner._tick` — pass `chunks_index=get_container().chunks_index_name` to `Reconciler(...)`. `src/ragent/bootstrap/app.py::_build_probes` — read `container.chunks_index_name` instead of literal `"chunks_v1"`. **Defaults on callee `__init__` remain** as test-convenience safety net (rename `VectorExtractor.index` → `chunks_index` deferred — naming nit, not a bug). `tests/unit/test_chunks_index_env_audit.py` (new) — 3 tests: composition threads env value through to `_FeedbackMemoryRetriever._chunks_index` and `VectorExtractor._index`; `_build_probes` queries `probe_es` with `[container.chunks_index_name]`. Reconciler `_tick` change is line-of-sight verified by existing integration tests under `tests/integration/test_reconciler_*.py`. | — | [x] | Dev |
| T-EI.2a | Red+Green | • **Achieve:** P1 reversal of B26 — flip `chunks_v1.embedding.index_options.type` from `flat` to `bbq_hnsw`. Fresh-install only; existing data on `flat` is out of scope (operator must wipe). Test resource keeps `flat` (CI light-weight); the structural-match invariant tolerates the delta alongside B42 ICU delta.<br>• **Deliver:** `resources/es/chunks_v1.json` flipped; `docs/00_spec.md §5.2` JSON mirror + "Vector index" prose updated; Decision Log row **B58** added; `tests/unit/test_init_schema.py` gains `test_prod_mapping_uses_bbq_hnsw_vector_index` (new pin) and renames `test_test_mapping_structurally_matches_prod_except_icu_deltas` → `test_test_mapping_structurally_matches_prod_except_documented_deltas` to absorb the new delta. | — | [x] | Dev |
| T-EI.2 | Red | • **Achieve:** Pin (a) ingest pipeline resource drift, (b) `indexed_at` mapping presence, (c) `default_pipeline` index setting.<br>• **Deliver:** `tests/integration/test_es_resource_drift.py` extended to load `resources/es/pipelines/chunks_default.json` and assert single `set` processor on `_ingest.timestamp → indexed_at`; `tests/unit/test_init_schema.py` extended to assert `chunks_v1.json` has `mappings.properties.indexed_at == {"type":"date"}` and `settings.index.default_pipeline == "chunks_default"`. `tests/integration/test_bootstrap_auto_init.py` extended: `GET _ingest/pipeline/chunks_default` returns 200 AND was created before the index (asserts via init_schema unit test on call order). Tests MUST fail before T-EI.3. | — | [ ] | QA |
| T-EI.3 | Green | • **Achieve:** Make T-EI.2 tests pass with minimal code.<br>• **Deliver:** new `resources/es/pipelines/chunks_default.json` (single `set` processor: `field: indexed_at`, `value: "{{{_ingest.timestamp}}}"`); `resources/es/chunks_v1.json` adds `"default_pipeline": "chunks_default"` under `settings.index` and `"indexed_at": {"type":"date"}` under `mappings.properties`. Mirror to `tests/resources/es/chunks_v1.json`. `src/ragent/bootstrap/init_schema.py::init_es` PUTs `_ingest/pipeline/chunks_default` from the resource file **before** PUT-ing the index; idempotent on existing pipeline. | T-EI.2 | [x] | Dev |
| T-EI.4 | Red+Green | • **Achieve:** End-to-end proof `indexed_at` is populated by ES (not by Python) — write a chunk doc whose `_source` does NOT carry `indexed_at`, GET it back, assert the field is present and parseable as ISO-8601 within the last 60s.<br>• **Deliver:** `tests/integration/test_bootstrap_auto_init.py` adds `test_chunk_write_populates_indexed_at_via_pipeline` (docker-gated) and `test_first_boot_creates_chunks_default_pipeline`. No Python writer code change required (value flows via the ingest pipeline). | T-EI.3 | [x] | QA |
| T-EI.5 | Spec | • **Achieve:** Spec §5.2 mirror catches up with the three schema changes; new Decision Log row records the B26-P1 reversal (`flat → bbq_hnsw`); B-row for the ingest-pipeline pattern references the 2026-05-19 journal entry.<br>• **Deliver:** `docs/00_spec.md §5.2` JSON block updated (default_pipeline + indexed_at + bbq_hnsw); §5.2 prose for "Vector index" rewritten (`flat` justification superseded); new paragraphs for "Default ingest pipeline `chunks_default`" (mirrored JSON), "Bootstrap order", and "`indexed_at` semantics" (last-write). New Decision Log rows **B58** (P1 bbq_hnsw flip — fresh-install, no reindex window) and **B59** (housekeeping fields via ES ingest pipeline pattern — cross-references journal). Spec drift test passes. | T-EI.3 | [x] | Spec |
| T-EI.6 | Red+Green | • **Achieve:** Close PR #83 review findings — extend `init_es` to honour `ES_CHUNKS_INDEX` when PUT-ing `chunks_v1.json` (gemini-code-assist high: T-EI.1 audit gap; bootstrap was still filename-stem-only); add `ES_CHUNKS_INDEX` to spec §4.6.2 inventory (B28 closed-system rule); amend B58 to cancel B26's bbq_hnsw fallback requirement (gemini-code-assist medium: dead code under ES 9.2.3+ guarantee); amend B59 with upgrade-path caveat (codex P1: operator wipe or manual `PUT _settings` + `_update_by_query` backfill); add new Decision Log **B60** for the init_es env-aware behaviour.<br>• **Deliver:** `src/ragent/bootstrap/init_schema.py::init_es` — one-line conditional: when `path.stem == "chunks_v1"`, use `os.environ.get("ES_CHUNKS_INDEX", "chunks_v1")`; otherwise stem. `tests/unit/test_init_schema.py` — `test_init_es_uses_env_chunks_index_name_for_chunks_resource` (env=`foo` ⇒ PUT to `/foo`) and `test_init_es_keeps_filename_stem_for_non_chunks_resources` (feedback_v1 still `/feedback_v1`). `docs/00_spec.md` — §4.6.2 row, B58 amendment, B59 amendment, B60 row. | PR #83 review | [x] | Dev |

### API pipeline param sanity & per-stage observability (branch `claude/api-pipeline-logging-bfjkJ`) — 2026-05-19

> Source: user audit 2026-05-19. Two themes: (a) parameter values declared on the HTTP contract (e.g. `top_k`) are honoured at the router boundary but a few internal Haystack components fall back to a build-time default mid-pipeline, causing extra upstream cost on the rerank path and ranking drift on the feedback path; (b) ingest pipeline steps emit `ingest.step.{started,ok,failed} + duration_ms` via `wrap_component_run`, but the chat / retrieve pipeline has no equivalent — operators cannot see where chat latency goes. Plus three smaller traps: `value or env_default` swallowing explicit `0` in client constructors (LLM/Embedding/Rerank timeouts + ES feedback `request_timeout`); module-level env reads in `services/ingest_service.py` + `routers/admin_ingest.py` + `pipelines/chat.py::_EXCERPT_MAX_CHARS` that bypass the composition-root DI seam; `request_id` generated by `RequestLoggingMiddleware` not propagated across the TaskIQ boundary so ingest worker logs cannot correlate with the originating HTTP request.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-APL.1 | Red | • **Achieve:** Pin that per-request `top_k` from `run_retrieval` reaches `_Reranker.run` and `_FeedbackMemoryRetriever.run` instead of being silently replaced by their construction-time `self._top_k`.<br>• **Deliver:** `tests/unit/test_chat_pipeline_components.py::test_reranker_run_accepts_runtime_top_k_overrides_construction_default` (rerank client called with the runtime `top_k`); `tests/unit/test_feedback_retriever.py::test_feedback_retriever_run_accepts_runtime_top_k_overrides_construction_default` (deduped-source cut trims at runtime `top_k`); `tests/unit/test_retrieve_router.py::test_run_retrieval_threads_top_k_to_reranker` + `…::test_run_retrieval_threads_top_k_to_feedback_retriever`. | §3.4.1 | [x] | QA |
| T-APL.2 | Green | • **Achieve:** Thread per-request `top_k` to both components — bug-fix only, HTTP contract unchanged.<br>• **Deliver:** `src/ragent/pipelines/chat.py::_Reranker.run` accepts `top_k: int \| None = None` falling back to `self._top_k`; `src/ragent/pipelines/chat.py::_FeedbackMemoryRetriever.run` same; `run_retrieval` adds `top_k` to `inputs["reranker"]` and `inputs["feedback_retriever"]` when not None. `docs/00_journal.md` row: "param threading — external contract honoured but internal pipeline used build-time default" lesson. | T-APL.1 | [x] | Dev |
| T-APL.3 | Red | • **Achieve:** Pin that explicit-`0` constructor kwargs on `LLMClient` / `EmbeddingClient` / `RerankClient` / `_FeedbackMemoryRetriever` are honoured instead of being swallowed by `value or os.environ.get(...)` falsy-checks.<br>• **Deliver:** `tests/unit/test_llm_client_chat.py` + `tests/unit/test_rerank_client.py` + `tests/unit/test_embedding_client.py` + `tests/unit/test_feedback_retriever.py` each gain a `test_explicit_zero_*timeout*_is_honoured` case asserting the stored attribute equals `0.0` (or that the ES `request_timeout` kwarg is forwarded). | §4.6 | [x] | QA |
| T-APL.4 | Green | • **Achieve:** Replace `value or env_default` with `value if value is not None else env_default` across the four constructors + `_search_kwargs()`.<br>• **Deliver:** `src/ragent/clients/{llm,embedding,rerank}.py` and `src/ragent/pipelines/chat.py::_FeedbackMemoryRetriever._search_kwargs`. | T-APL.3 | [x] | Dev |
| T-APL.5 | Structural | • **Achieve:** Move `_MAX_INLINE_BYTES` / `_MAX_FILE_BYTES` / `_LIST_MAX` (`services/ingest_service.py`) + `_MAX_UPLOAD_BYTES` (`routers/admin_ingest.py`) + `_EXCERPT_MAX_CHARS` (`pipelines/chat.py`) reads to the composition root and inject them as constructor kwargs, so the "composition root is the single DI seam" rule (CLAUDE.md) holds for these consumers too.<br>• **Deliver:** `src/ragent/bootstrap/composition.py` reads the env vars; `IngestService.__init__` + admin upload router factory + `_ExcerptTruncator.__init__` already accept the values — call sites pass them explicitly; existing module-level constants removed. No HTTP contract change. | §4.6 | [x] | Dev |
| T-APL.6 | Red | • **Achieve:** Pin that every chat pipeline component (vector retriever, BM25 retriever, joiner, reranker, source hydrator, excerpt truncator, feedback retriever when enabled) emits a `chat.step.{started,ok,failed}` event carrying `step`, `duration_ms`, and the same context inheritance from `structlog.contextvars` the ingest pipeline uses.<br>• **Deliver:** `tests/unit/test_chat_pipeline_observability.py` (new) — per-component asserts using `structlog.testing.capture_logs`. | §4.4 | [x] | QA |
| T-APL.7 | Green | • **Achieve:** Extract `wrap_component_run` from ingest-only into a generic `wrap_pipeline_component(*, namespace, step, error_code)` helper and apply it to all chat pipeline components at `build_retrieval_pipeline` time. `request_id` / `user_id` propagation to chat-step logs already covered by `RequestLoggingMiddleware`'s `structlog.contextvars.bind_contextvars`, so a separate `bind_chat_context` helper is unnecessary (the wrapper reads contextvars set upstream).<br>• **Deliver:** `src/ragent/pipelines/observability.py` (new generic helper; `wrap_component_run` kept as thin ingest-namespace alias for back-compat); `src/ragent/pipelines/chat.py::build_retrieval_pipeline` wraps each component via local `_add` helper before `pipeline.add_component`. | T-APL.6 | [x] | Dev |
| T-APL.8 | Red | • **Achieve:** Pin that the `request_id` bound by `RequestLoggingMiddleware` survives across the TaskIQ enqueue/execute boundary so worker `ingest.*` logs share the originating HTTP request's correlation id.<br>• **Deliver:** `tests/unit/test_taskiq_request_id_middleware.py` — pre_send snapshots `request_id`/`user_id` from `structlog.contextvars` into `message.labels`; pre_execute rebinds; post_execute clears to avoid leaking across tasks on the same worker coroutine; InMemoryBroker e2e round-trip per the journal 2026-05-06 rule. | §4.4 | [x] | QA |
| T-APL.9 | Green | • **Achieve:** Implement `taskiq.TaskiqMiddleware` subclass propagating `request_id` (and `user_id`) end-to-end and register on the global broker.<br>• **Deliver:** `src/ragent/middleware/taskiq_context.py::StructlogContextMiddleware`; `src/ragent/bootstrap/broker.py` calls `broker.add_middlewares(...)` so api producer and worker consumer both see it; `docs/00_spec.md §4.4` notes the cross-process correlation contract. | T-APL.8 | [x] | Dev |
| T-APL.10 | Structural | • **Achieve:** Drop the `wrap_component_run` back-compat alias in `pipelines/observability.py`; existing ingest factory call sites + tests migrate to `wrap_pipeline_component(namespace="ingest", ...)` directly. Test files keep a file-local `_wrap` shim for readability, NOT a re-exported alias.<br>• **Deliver:** `src/ragent/pipelines/factory.py::build_ingest_pipeline` uses a local `_add` closure + explicit namespace; `src/ragent/pipelines/observability.py` deletes the alias function; `tests/unit/test_pipeline_logging.py` adds a `_wrap` shim and migrates all 6 call sites; `tests/unit/test_chat_pipeline_observability.py` renames the backcompat-named test to assert via the public API; `src/ragent/errors/codes.py` comment renames. No HTTP contract change. | T-APL.7 | [x] | Dev |
| T-APL.11 | Red+Green | • **Achieve:** Each wrapped `run()` opens an OTEL span named `{namespace}.step.{step}` alongside the structlog event, so traced deployments (Jaeger / Tempo / OTel collector) get a waterfall view of per-step durations without re-instrumenting any component. NoOp tracer is the fallback when `setup_tracing()` is not called — un-traced processes pay only the context-manager enter/exit cost.<br>• **Deliver:** `src/ragent/pipelines/observability.py::wrap_pipeline_component` opens `_tracer.start_as_current_span(f"{namespace}.step.{step}")` with attributes `{pipeline.namespace, pipeline.step, atoms_in?, chunks_out?, duration_ms}`; on exception calls `span.record_exception` + `span.set_status(StatusCode.ERROR, error_code)` before re-raise. `tests/unit/test_chat_pipeline_observability.py` adds `test_wrap_pipeline_component_emits_otel_span_per_call` + `…_otel_span_records_failure` using the `otel_exporter` fixture. `docs/00_spec.md §4.4` notes the dual-emission (structlog log + OTEL span). | T-APL.7, T-APL.10 | [x] | Dev |
